# QFramework 修改计划

> 2026-08-05 更新：第 1～11 章是已经实施完成的“跨进程有界队列与日志
> 100 ms 刷新”历史计划，保留它们用于追溯，不得在新会话中重复实施。
> 本次新的代码修改从第 12 章开始；如果前后章节存在范围冲突，以第 12 章
> 及之后的内容为准。

## 1. 文档状态

- 日期：2026-08-04。
- 状态：历史计划，相关 IPC/日志改造已经完成并通过当时的构建与测试；保留记录。
- 文档类型：代码修改执行计划。
- 目标读者：不熟悉现代 C++、主要使用 Qt 进行开发和维护的开发者。
- 工作区：`D:\QFrameworkv2`。
- 本文件描述计划，不代表源码已经修改，也不代表构建和测试已经通过。

## 2. 本次目标

本次只完成以下两个修改：

1. 帧流消息达到队列容量后覆盖同主题的最旧帧，避免持续积压。
2. 日志改为每 100 ms 批量写入并刷新一次，不再每条日志都调用 `QFile::flush()`。

实现必须优先使用 Qt 5.15.2 提供的类型和线程机制，例如：

- `QQueue`、`QHash`、`QByteArray`；
- `QMutex`、`QMutexLocker`、`QWaitCondition`；
- `QThread`、signal/slot、`Qt::QueuedConnection`；
- `QElapsedTimer`；
- `QLocalSocket`、`QSharedMemory`；
- `QJsonObject`、`QJsonArray`、`QSettings`；
- `QFile`、`QSaveFile`。

不引入 `std::thread`、`std::mutex`、复杂模板、协程或锁自由队列。保留现有 Qt/C++17 风格，不进行无关重构。

## 3. 已确认的现状

### 3.1 主进程消息总线已经支持 Latest

`Source\QFramework\Messaging\MessageBus.cpp` 已经按订阅者和主题执行以下策略：

- `QueuePolicy::Latest`：容量满时删除最旧消息，再加入新消息；
- `QueuePolicy::Reliable`：容量满时拒绝新消息，`publish()` 返回 `false`。

`config\QFramework.ini` 中的 `QFRAMEWORK_IMAGE_RAW` 已配置为：

```ini
[Topic.QFRAMEWORK_IMAGE_RAW]
QueueCapacity=4
MaxMessageBytes=16777216
Policy=Latest
```

因此本次不重复修改主消息总线已有的 Latest 规则，重点补齐跨进程传输中的无上限暂存点。

### 3.2 跨进程链路仍有无上限队列

当前父进程到子进程的数据路径为：

```text
MessageBus 有界队列
    -> ProcessBridge 每条消息产生一个 Qt queued event
    -> QLocalSocket / QSharedMemory
    -> ProcessRuntime 无界 MessageQueue
    -> 模块 onMessage()
```

当前子进程到父进程的数据路径为：

```text
模块 publish()
    -> ProcessRuntime 每条消息产生一个 Qt queued event
    -> QLocalSocket / QSharedMemory
    -> 父进程 MessageBus
```

所以主消息总线即使有容量限制，消息仍可能被搬到无容量限制的 Qt 事件队列或子进程消息队列中。

### 3.3 日志当前逐条刷新

`Source\QFramework\Logging\Logger.cpp` 当前每处理一条记录都会执行：

```cpp
file_.write(bytes);
file_.flush();
```

这会降低批量写入效率。本次只修改刷新频率，不处理日志队列无上限、旧日志清理和日志停止超时。

## 4. 行为约束

### 4.1 Latest 和 Reliable 必须严格区分

| 策略 | 达到容量后的行为 |
| --- | --- |
| `Latest` | 删除同主题最旧帧，加入最新帧，并增加覆盖计数 |
| `Reliable` | 不覆盖旧消息；没有空位时拒绝或等待可用槽位，但等待必须可以被停止和超时唤醒 |

不能对状态命令、控制命令等 Reliable 消息使用“覆盖旧消息”的方式。

### 4.2 覆盖粒度

本次按 `topic` 作为覆盖粒度，与现有 `MessageBus` 行为一致。

如果四路摄像头共用同一个主题，队列容量 4 表示四路共用四个槽位；高速通道可能覆盖其他通道的旧帧。需要严格保证每路至少保留一帧时，应为四路摄像头使用独立主题。框架本次不解析通用 `QByteArray` 内部的摄像头通道字段。

### 4.3 publish() 返回值

本次不修改公开 SDK 中 `publish()` 的同步接口：

- 主进程模块仍返回中央消息总线的接收结果；
- 子进程模块仍返回“子进程本地发送队列是否接收”的结果；
- 父进程最终拒绝子进程消息时，通过内部 ACK、计数和日志记录，不把同步等待引入模块调用线程。

### 4.4 所有新增等待都必须可结束

新增的 `QWaitCondition` 等待必须满足：

- 使用明确超时；
- 模块停止、Socket 断开、子进程故障或重启时调用 `wakeAll()`；
- 不得新增无参数 `QThread::wait()` 或无法被停止的阻塞等待；
- 不使用 `Qt::BlockingQueuedConnection`。

## 5. 预计修改文件

### 5.1 帧队列与跨进程传输

- `Source\QFramework\Process\ProcessSupervisor.cpp`
- `Source\QFramework\Process\ProcessSupervisor.h`
- `Source\QFramework\Process\ProcessRuntime.cpp`
- `Source\QFrameworkSdk\ProcessRuntime.h`
- `Tests\QFrameworkTests\tst_Baseline.cpp`
- `Tests\QFrameworkTests\tst_Baseline.h`

优先在现有 `.cpp` 中增加小型内部结构，不新增源文件，避免扩大 `.vcxproj` 修改范围。

### 5.2 日志 100 ms 刷新

- `Source\QFramework\Logging\Logger.cpp`
- `Source\QFramework\Logging\Logger.h`
- `Source\QFramework\Config\FrameworkConfig.cpp`
- `Source\QFramework\Config\FrameworkConfig.h`
- `Source\QFramework\Core\FrameworkRuntime.cpp`
- `config\QFramework.ini`
- `Docs\Reference\框架配置文件参考.md`
- `Docs\Explanation\架构说明.md`
- `Tests\QFrameworkTests\tst_Baseline.cpp`
- `Tests\QFrameworkTests\tst_Baseline.h`

## 6. 实施批次

### 批次 1：先补充回归测试入口

目标：先固定本次行为，防止只修改代码却没有可重复验证方法。

步骤：

1. 在 `tst_Baseline.h` 中增加测试槽函数。
2. 在 `tst_Baseline.cpp` 中增加慢消费者测试辅助逻辑，不修改四个正式示例模块的业务行为。
3. 增加父进程到子进程的 Latest 测试：容量设为 1，快速发送序号 1 到 100，子进程故意慢速接收。
4. 增加子进程到父进程的 Latest 测试。
5. 增加 Reliable 容量测试，确认第二条消息不会覆盖第一条。
6. 增加日志批量刷新测试入口。

建议测试名称：

```text
processLatestQueueOverwritesOldFrames
processReliableQueueDoesNotOverwrite
loggerFlushesAtConfiguredInterval
```

通过条件：测试能够在旧实现上暴露跨进程积压或缺少 100 ms 刷新机制，且不会依赖真实摄像头和硬件。

### 批次 2：父进程到子进程使用有界 Qt 队列

目标：删除 `ProcessBridge` 每帧创建一个 Qt queued event 的无界路径。

建议实现：

1. 在 `ProcessSupervisor.cpp` 中定义简单的内部消息结构，字段只包含：

```text
topic
senderModuleId
data
messageId
```

2. 每个 `ProcessSupervisor::Entry` 保存：

- `QQueue` 待发送消息；
- 每个主题的待发送数量；
- 每个主题的在途数量；
- Latest 覆盖计数；
- Reliable 拒绝计数；
- 一个“是否已经安排发送”的布尔标记；
- 用于保护这些字段的 `QMutex` 和 `QWaitCondition`。

3. `ProcessBridge::onMessage()` 不再为每帧调用一次 `QMetaObject::invokeMethod()`传递完整 `QByteArray`。
4. `onMessage()` 只调用线程安全的入队函数：

```text
锁住队列
    -> 读取该主题 TopicConfig
    -> Latest 满：删除同主题最旧帧
    -> Reliable 满：不覆盖
    -> 加入新消息
    -> 如果尚未安排发送，只安排一次 Qt queued 唤醒
    -> 解锁并立即返回
```

5. 主线程槽函数每次从 `QQueue` 中取有限数量消息发送，不能一次把全部消息重新写入 `QLocalSocket` 缓冲区。
6. 使用在途窗口限制尚未收到 ACK 的消息数量。
7. 对 inline 和 shared 两种消息都携带 `messageId`。
8. Socket 断开、模块停止、故障和重启时清理队列并调用 `wakeAll()`。

通过条件：ProcessBridge 不再产生“一帧对应一个携带完整图像数据的 Qt 事件”。

### 批次 3：子进程输入和发送队列有界化

目标：让父子进程两侧都遵守相同的 TopicConfig。

步骤：

1. 父进程在 `registerAck` 中增加模块相关主题配置，使用 `QJsonArray` 传递：

```text
topic
queueCapacity
maxMessageBytes
policy
```

2. `ProcessRuntime` 使用 `QHash<QString, TopicConfig>` 保存主题配置。
3. 修改 `ProcessRuntime::MessageQueue::enqueue()`：

- 统计队列中同主题消息数；
- Latest 满时用 `QQueue::removeAt()`删除同主题最旧消息；
- Reliable 满时返回 `false`；
- 加入新消息后使用 `QWaitCondition::wakeOne()`。

4. 子进程完成队列接收后发送内部 delivery ACK。
5. 父进程收到 ACK 后：

- 释放对应共享内存；
- 减少在途计数；
- 唤醒待发送逻辑。

6. 将 `ProcessRuntime::queuePublish()` 的“一条消息一个 Qt event”改为有界发送队列加一次合并唤醒。
7. 父进程调用 `MessageBus::publishFromModule()` 后发送内部 publish ACK，使子进程释放发送槽位和共享内存。
8. ACK 被拒绝时记录限频警告和计数，不能在高频帧下逐帧写警告日志。

通过条件：慢消费者场景下，父进程和子进程的待处理帧数始终受 `QueueCapacity` 和在途窗口限制。

### 批次 4：日志每 100 ms 批量刷新

目标：保留异步日志线程，但删除逐条 `QFile::flush()`。

配置调整：

```ini
[Logging]
FlushIntervalMs=100
```

`LoggingConfig` 增加：

```text
flushIntervalMs，默认 100
```

非法值或缺失配置回退为 100。

建议实现：

1. `Logger::Worker` 构造函数接收刷新间隔。
2. 使用现有 `QQueue<LogRecord>` 保存待写日志。
3. 使用 `QElapsedTimer` 记录距离上次刷新经过的时间。
4. 使用带超时的 `QWaitCondition::wait()` 等待新日志或下一个刷新时刻。
5. 到达 100 ms 时取出一批日志并写入文件，整批只执行一次 `QFile::flush()`。
6. 日志持续产生时也必须检查刷新截止时间，不能只有队列空闲时才刷新。
7. 以下场景绕过 100 ms 等待并立即刷新：

- 显式调用 `Logger::flush()`；
- Qt Fatal 日志；
- 日志文件换日；
- 达到 `MaxFileBytes` 发生文件滚动；
- `Logger::stop()`。

8. 保留旧 `Logger::start(directory, maxFileBytes, errorMessage)`，让它使用默认 100 ms；增加带刷新间隔的重载供 `FrameworkRuntime` 使用。

通过条件：`Logger.cpp` 的普通单条日志路径不再调用 `file_.flush()`，持续日志下每秒约刷新 10 次，而不是每条刷新一次。

### 批次 5：文档、构建与完整回归

文档更新：

1. `Docs\Reference\框架配置文件参考.md` 增加 `FlushIntervalMs`。
2. `Docs\Explanation\架构说明.md` 说明 Latest 覆盖、跨进程有界队列、ACK 和 100 ms 日志刷新。
3. 如公开 SDK 的 `publish()` 说明与实际语义不一致，同步修正 `Docs\Reference\模块开发接口参考.md`，但不改变本批公开 API。

本批不执行 qmake、VS2019 或 v142 迁移，仍使用当前仓库已有构建基线验证，避免把构建体系迁移混入这两个行为修改。

## 7. 测试与验收标准

### 7.1 帧覆盖验收

- Latest 容量为 1，连续发送 1 到 100，慢消费者最终必须收到最新序号 100。
- 消费者不能依次处理全部 100 帧；测试必须观察到旧帧被覆盖。
- `dropped/overwritten` 计数大于 0。
- inline 小消息和 QSharedMemory 大消息都通过。
- 父到子和子到父两个方向都通过。
- Reliable 容量满时不覆盖，发布方能观察到拒绝或内部拒绝统计。
- 同一发送者、同一主题中实际交付的帧顺序不倒序。
- 子进程停止、断开和重启后，待发送队列和共享内存记录被清理。
- 队列满时程序仍可在配置的停止时间内退出，新增代码不得出现无限等待。

### 7.2 日志验收

- `FlushIntervalMs` 缺失时默认 100。
- 非法值回退为 100。
- 单条日志在合理调度误差内完成刷新，测试等待上限建议为 500 ms，避免时间测试不稳定。
- 持续高频日志时仍约每 100 ms 刷新，不等队列为空。
- `Logger::flush()` 调用返回后，调用前的日志都可以从文件读取。
- `Logger::stop()` 不丢失最后一批日志。
- 日志换日和按大小滚动继续通过。
- Qt 全局日志捕获继续通过。

### 7.3 回归验收

- 配置文件读取后内容不被程序改写。
- 主进程模块加载、子进程注册、心跳、重启和窗口嵌入测试通过。
- Dock 和布局测试通过。
- `git diff --check` 通过。

## 8. 构建与测试命令

先运行最快的相关 Debug x64 构建：

```powershell
& 'D:\Program Files (x86)\Microsoft Visual Studio\2017\Community\MSBuild\15.0\Bin\MSBuild.exe' D:\QFrameworkv2\QFramework.sln /m /p:Configuration=Debug /p:Platform=x64 /v:minimal
```

运行全部 Qt Test：

```powershell
D:\QFrameworkv2\Bin\v141\x64\Debug\QFrameworkTests.exe
```

也可以先运行新增和直接相关的测试：

```powershell
D:\QFrameworkv2\Bin\v141\x64\Debug\QFrameworkTests.exe processLatestQueueOverwritesOldFrames processReliableQueueDoesNotOverwrite loggerFlushesAtConfiguredInterval messageBusPoliciesAndOrdering processIpcAndSupervision loggerRollsAndCapturesQtMessages
```

检查补丁格式：

```powershell
git -C D:\QFrameworkv2 diff --check
```

如 Debug x64 全部通过，再运行 Release x64 构建和完整测试，确认日志批量刷新在优化构建下没有行为差异。

## 9. 本次明确不做的内容

- 不修复 `MessageBus::finishStop()` 超时后再次无限 `wait()` 的既有问题。
- 不增加日志队列条数或字节数上限。
- 不增加旧日志保留天数、文件数或总容量清理。
- 不实现完整插件安装、启用、禁用和卸载系统。
- 不增加中央工作区。
- 不迁移 qmake、VS2019 或 v142。
- 不修改 Dock 拖拽和左侧侧边栏相关代码。
- 不把四路摄像头数据协议解析写进通用消息框架。

## 10. 回滚方案

建议形成两个独立提交：

1. 跨进程 Latest 有界队列、ACK 和对应测试。
2. 日志 100 ms 批量刷新、配置、文档和对应测试。

两项互不依赖。出现问题时优先按提交整体回滚，不在运行目录手工替换单个 DLL，避免主进程和子进程使用不同 IPC 协议。

## 11. 新会话实施提示词

```text
请在 D:\QFrameworkv2 中执行代码修改。这是代码实施任务，不是重新分析需求，也不是重写计划。

开始前：
1. 完整阅读 D:\QFrameworkv2\修改计划.md，并严格以该文件为本次范围。
2. 检查仓库内 AGENTS.md、git status 和现有 diff，保留用户已有修改，不回滚或覆盖无关内容。
3. 重新核对当前 ProcessSupervisor、ProcessRuntime、MessageBus、Logger、FrameworkConfig、FrameworkRuntime 和 Qt Test 实现，确认计划与最新代码没有漂移。

实施要求：
1. 按修改计划.md的批次顺序完成全部代码、配置、文档和测试，不要停在再次给计划。
2. 帧流 Latest 达到 QueueCapacity 后覆盖同主题最旧帧；Reliable 消息绝不覆盖。
3. 补齐父到子和子到父的跨进程有界队列，避免每帧携带完整 QByteArray 产生一个无上限 Qt queued event；限制在途消息和共享内存，使用内部 ACK 释放槽位。
4. 日志增加 FlushIntervalMs=100，普通日志每 100 ms 批量写入并只 flush 一次；显式 flush、Fatal、滚动和停止仍立即刷新。
5. 尽量使用 Qt 5.15.2 API：QQueue、QHash、QByteArray、QMutex、QMutexLocker、QWaitCondition、QThread、QElapsedTimer、signal/slot、QLocalSocket、QSharedMemory、QJsonObject、QJsonArray、QSettings 和 QFile。不要引入 std::thread、std::mutex、复杂模板、协程或锁自由队列。
6. 保持小范围、可审查的修改；优先在现有 cpp/h 中实现，不做无关重构，不新增网络调用、遥测或依赖。
7. 所有新增等待必须有超时并能在停止、断开、故障和重启时被唤醒；不得使用 Qt::BlockingQueuedConnection。
8. 本次不要修改 MessageBus 既有无限 wait 问题、日志保留策略、完整插件系统、中央工作区、Dock、侧边栏或构建体系。

验证要求：
1. 增加并运行 Latest 覆盖、Reliable 不覆盖、inline/shared、双向 IPC、100 ms 日志刷新、显式 flush 和停止不丢日志测试。
2. 使用当前仓库构建基线完成 Debug x64 构建和全部 QFrameworkTests；通过后再完成 Release x64 验证。
3. 运行 git diff --check。
4. 最终报告修改摘要、修改文件、测试与构建命令及结果，并明确任何未执行的硬件或长时间人工验证。

不要只给建议；请持续实施、构建和测试，直到本计划范围完成或出现有证据的外部阻塞。
```

---

## 12. 2026-08-05 新修改计划状态

- 日期：2026-08-05。
- 状态：待实施。
- 文档类型：代码修改执行计划。
- 工作区：`D:\QFrameworkv2`。
- 目标读者：长时间没有使用 C++、更熟悉 Qt 类和 signal/slot 的维护者。
- 本轮目标：完成问题 2、3、4、7、8、9、11、12、13、14、15，以及
  `修改建议.txt` 中“🔧 小毛病”的全部 7 项。
- 第 1～11 章已经完成的 Latest/Reliable 有界队列和日志 100 ms 刷新不是
  本轮工作，不得重复重写或回退。
- 本文件现在只是计划；只有实际源码、测试、构建和文档同步完成后，才能把
  状态改为“已完成”。

### 12.1 本轮问题清单

1. 问题 2：模块菜单只能显示 Dock，取消勾选不能隐藏。
2. 问题 3：标签布局保存后，非当前标签被误存成隐藏，重新加载后标签组拆散。
3. 问题 4：消息回调卡死时，框架退出可能在无参数 `QThread::wait()` 永久等待。
4. 问题 7：布局先恢复、子进程窗口后 ready 时，布局要求显示的窗口不会自动补显示。
5. 问题 8：手动重启同步等待，阻塞主窗口。
6. 问题 9：切换标签时 `visibilityChanged(false)` 误取消菜单勾选。
7. 问题 11：模块停止期间仍接受父进程发来的新消息。
8. 问题 12：等待 A 停止时通过 `processEvents()` 处理 B 的重启，破坏 A 的停止 deadline。
9. 问题 13：子进程消息线程超时后使用 `QThread::terminate()` 强杀。
10. 问题 14：`ProcessRuntime::running_` 和 `QLocalSocket::state()` 被业务线程跨线程读取。
11. 问题 15：SDK 对 `onStart()` 期间是否允许 `publish()` 的注释与实现相反。
12. 小问题：死变量、两个无等待者的条件变量、控制帧写失败、停止丢弃统计、
    重启窗口边界和错误缩进。

### 12.2 本轮明确不自动扩大到的内容

- 问题 16：示例模块未检查 `publish()` 返回值，本轮不作为必改项。
- “界面设计不合理”章节中的侧边栏、快捷键、弹窗策略和状态中文化不作为
  本轮必改项。
- 不重做已经完成的 Latest/Reliable 队列、ACK、共享内存和日志 100 ms 刷新。
- 不迁移 VS2017/v141、Qt 5.15.2、工程文件格式或构建体系。
- 不做全仓无关重构、无关变量改名或为了“看起来更新”而改写无关文档。

## 13. 实施语言、代码风格和注释要求

### 13.1 尽量使用 Qt 语言

优先使用项目已经采用的 Qt 类型和机制：

- 容器和值类型：`QHash`、`QSet`、`QQueue`、`QVector`、`QString`、`QByteArray`；
- 并发保护：`QMutex`、`QMutexLocker`、`QWaitCondition`；
- 生命周期时间：`QTimer`、`QElapsedTimer`、`QDeadlineTimer`；
- 对象与异步通知：`QObject`、signal/slot、`QMetaObject::invokeMethod()`；
- UI 状态：`QAction`、`QSignalBlocker`、`QDockWidget`、`QCloseEvent`；
- 进程和 IPC：`QProcess`、`QLocalServer`、`QLocalSocket`、`QSharedMemory`；
- 文件和 JSON：`QSaveFile`、`QJsonObject`、`QJsonDocument`。

不要为本轮引入 `std::thread`、`std::mutex`、协程、复杂模板、无锁队列或
新的第三方依赖。Qt 没有提供“安全强制终止一条卡死业务线程并继续运行”的
能力；只有最终进程级 fail-fast 边界允许使用一个集中封装的平台 API，并必须
写清楚原因。

### 13.2 状态机写法

不要求引入 Qt State Machine 模块。优先在现有 `QObject` 中使用：

- 一个明确的 `enum class` 表示生命周期阶段；
- `QTimer` 或 `QDeadlineTimer` 表示 deadline；
- QProcess/Socket 信号推进阶段；
- 小型私有函数集中执行状态转换；
- 不用多个互相独立的布尔值猜测当前阶段。

### 13.3 中文注释规则

新增和修改代码应补充简明中文注释，重点解释：

- “用户希望显示”和“Qt 此刻实际可见”为什么不能混用；
- QObject、QLocalSocket、QProcess 分别属于哪个线程；
- 停止闸门为什么必须在发送 stop 之前关闭；
- 每个状态机阶段由哪个 Qt 信号进入、由哪个 deadline 退出；
- 为什么不能使用 `QThread::terminate()`；
- 超时后为什么必须跳过 DLL 卸载；
- 控制帧发送失败后为什么立即进入故障流程。

不要给显而易见的赋值、简单循环和普通 getter 逐行添加注释。注释应帮助
久未使用 C++ 的维护者理解“为什么这样写”，而不是把 C++ 语句翻译成中文。

## 14. 已确认的数据流和根因

### 14.1 Dock 显示状态

当前把三个不同概念混成了 `QDockWidget::isVisible()`：

```text
用户/布局希望显示
        +
模块 QWidget 或 HWND 已经 ready
        +
Qt 此刻正在绘制这个标签
```

正确关系应改成：

```text
requestedVisible（用户意图）
        +
uiAvailable（模块界面是否 ready）
        -> 决定是否实际 show/hide

QDockWidget::isVisible()
        -> 只用于观察 Qt 当前绘制状态
        -> 不反向修改用户意图
```

### 14.2 子进程重启

当前同步流程：

```text
点击重启
 -> stop()
 -> GUI 线程循环 processEvents()
 -> terminate/kill
 -> startEntry()
 -> waitForStarted()/waitForRunning()
 -> 返回 GUI
```

目标异步流程：

```text
点击重启
 -> requestRestart() 立即返回
 -> 关闭消息入口
 -> QProcess/Socket 信号推进停止
 -> 旧进程退出
 -> QTimer 安排新启动
 -> register/started 信号推进到 Running
 -> restartFinished() 通知 UI
```

### 14.3 卡死回调

`onMessage()` 已经在队列锁外执行，因此清空等待队列只能删除尚未执行的消息，
不能打断当前已经进入业务代码的回调。强杀线程可能让它持有的 `QMutex` 永久
不解锁，也不能在该线程仍执行 DLL 代码时卸载 DLL。

因此必须采用：

```text
协作停止标志
 -> 有限等待
 -> 超时后隔离对象并跳过卸载
 -> 子进程结束整个 QProcess
 -> 主进程退出阶段使用进程级 fail-fast
```

## 15. 预计修改文件

### 15.1 Dock、布局和模块管理

- `Source\QFramework\Ui\MainWindow.h`
- `Source\QFramework\Ui\MainWindow.cpp`
- `Source\QFramework\Ui\ManagedDockWidget.h`
- `Source\QFramework\Ui\ManagedDockWidget.cpp`
- `Source\QFramework\Ui\LayoutManager.h`
- `Source\QFramework\Ui\LayoutManager.cpp`
- `Source\QFramework\Ui\ModuleManagerDialog.h`
- `Source\QFramework\Ui\ModuleManagerDialog.cpp`

### 15.2 消息、进程和关闭流程

- `Source\QFramework\Messaging\MessageBus.h`
- `Source\QFramework\Messaging\MessageBus.cpp`
- `Source\QFramework\Process\ProcessSupervisor.h`
- `Source\QFramework\Process\ProcessSupervisor.cpp`
- `Source\QFramework\Process\ProcessRuntime.cpp`
- `Source\QFrameworkSdk\ProcessRuntime.h`
- `Source\QFrameworkSdk\ModuleEndpoint.h`
- `Source\QFramework\Plugin\ModuleEndpoint.cpp`
- `Source\QFramework\Plugin\PluginManager.h`
- `Source\QFramework\Plugin\PluginManager.cpp`
- `Source\QFramework\Core\FrameworkRuntime.h`
- `Source\QFramework\Core\FrameworkRuntime.cpp`
- 必要时修改 `Source\QFrameworkApp\main.cpp`，但只用于集中处理最终 fail-fast 结果。

### 15.3 测试

- `Tests\QFrameworkTests\tst_Baseline.h`
- `Tests\QFrameworkTests\tst_Baseline.cpp`

优先复用测试程序已有的子进程双身份入口，不新增源文件，避免无必要修改
`.vcxproj`。

### 15.4 Markdown 文档

实施时必须核查仓库中全部受版本控制的 `.md` 文件，当前清单见第 22 章。

## 16. 批次 1：用户显示意图与布局恢复

覆盖问题：2、3、7、9。

> 批次状态（2026-08-05）：代码、7 项直接 Debug x64 Qt Test 和本批相关文档
> 已完成；完整 Debug/Release、真实桌面人工验收仍按第 24 章在最终收口执行。

### 16.1 MainWindow 状态拆分

在 `MainWindow` 增加：

```cpp
QHash<QString, bool> requestedDockVisibility_;
```

保留现有：

```cpp
QHash<QString, bool> uiAvailable_;
```

新增小型私有函数，名称可根据现有风格调整：

```text
setRequestedDockVisible(moduleId, visible, origin)
applyRequestedDockVisibility(moduleId, origin)
syncModuleAction(moduleId)
```

`origin` 至少区分：

- 用户菜单操作：显示时允许 `raise()`；
- 布局恢复：不得抢走布局保存的当前标签；
- 模块 ready：自动补显示，但不得无理由抢焦点；
- 用户关闭按钮：更新意图为隐藏。

### 16.2 菜单显示和隐藏

1. 把模块 QAction 连接到带 `bool checked` 参数的槽。
2. `checked=true`：
   - 写入 `requestedDockVisibility_[moduleId]=true`；
   - ready 时显示并激活；
   - 未 ready 时保留 pending。
3. `checked=false`：
   - 写入 false；
   - 立即 hide；
   - 之后 windowReady 也不能自动显示。
4. 使用 `QSignalBlocker` 同步菜单状态，避免程序更新 QAction 时反向触发用户槽。

### 16.3 区分关闭按钮和标签切换

1. `ManagedDockWidget` 覆盖 `closeEvent(QCloseEvent*)`。
2. 增加只表示用户关闭请求的信号。
3. `MainWindow` 收到该信号后取消显示意图和菜单勾选。
4. 删除或停止使用 `visibilityChanged(false) -> action->setChecked(false)`。
5. 标签切换只改变 Qt 实际可见状态，不改变显示意图。

### 16.4 布局保存和加载

1. `LayoutManager::saveLayout()` 接收显示意图快照，不再直接把
   `dockWidget->isVisible()` 当作用户意图。
2. 新布局在模块对象中写入 `requestedVisible`。
3. 为兼容旧程序，同时保留 `visible`，值与 `requestedVisible` 一致。
4. 根对象增加可选标识，例如 `visibilitySemantics=userIntent`。
5. 新版本加载时优先读取 `requestedVisible`；旧布局没有该字段时读取 `visible`。
6. 旧布局中主动隐藏和非当前标签已经无法可靠区分，不做可能错误的强制猜测；
   加载成功后提示用户重新确认并保存。
7. `restoreState()` 继续负责 Dock 位置、标签关系、尺寸和当前标签。
8. MainWindow 只在整个布局验证和恢复成功后替换显示意图，加载失败时保留
   原显示意图和原布局。

### 16.5 子进程窗口晚到

1. 布局要求显示但 `uiAvailable=false` 时：
   - QAction 保持勾选；
   - Dock 暂时隐藏；
   - 显示意图保留。
2. `onWindowHandleReady()` 完成 HWND attach 和 `showWindow()` 后：
   - 设置 `uiAvailable=true`；
   - 应用 pending 显示意图。
3. `Starting/Restarting/Failed/Stopped` 时：
   - 设置 `uiAvailable=false`；
   - 显示占位信息；
   - 不改变显示意图。
4. `releaseInProcessUiModules()` 同样要更新 `uiAvailable=false`。

### 16.6 本批测试

- `moduleMenuTogglesRequestedVisibility`
- `dockCloseClearsRequestedVisibility`
- `tabSwitchKeepsRequestedVisibility`
- `layoutPreservesTabbedVisibilityIntent`
- `layoutDefersUnavailableDockUntilReady`
- `layoutHiddenDockStaysHiddenAfterReady`
- `legacyLayoutLoadsWithoutCorruptingCurrentState`

关键断言：

- 取消菜单勾选后 Dock 隐藏；
- 两个标签切换后两个 QAction 仍保持勾选；
- 保存/加载后 `tabifiedDockWidgets()` 仍包含另一个 Dock；
- pending=true 在 ready 后自动显示；
- pending=false 在 ready 后保持隐藏；
- 布局失败时位置、标签、显示意图和活动文件路径全部回滚。

### 16.7 本批文档同步检查点

- 更新 `修改计划.md` 本批勾选状态。
- 在 `修改进度.md` 追加实际修改、测试和未验证项。
- 同步 `Docs\Guides\操作与调试指南.md` 的菜单、标签和布局说明。
- 同步 `Docs\Explanation\架构说明.md` 的显示意图与实际可见状态。
- 核查 `Docs\Reference\框架配置文件参考.md` 的 Layout 说明。

### 16.8 实际实施结果

- `MainWindow` 使用 `requestedDockVisibility_` 保存用户/布局意图；QAction 取消勾选
  会隐藏 Dock，标签切换不再反向修改 QAction。
- `ManagedDockWidget::closeRequested()` 只在关闭事件中撤销显示意图。
- `.qflayout` 同时保存兼容字段 `visible` 和新字段 `requestedVisible`，并以
  `visibilitySemantics=userIntent` 标识语义；旧布局继续读取 `visible`。
- `uiAvailable=false` 时保留 pending 意图，窗口 ready 后按意图补显示；明确隐藏
  的 Dock 不补显示。
- 第 16.6 节列出的 7 项测试均已实际运行并通过。完整命令、环境告警和剩余门槛
  记录在 `修改进度.md`。

## 17. 批次 2：异步停止和重启状态机

覆盖问题：8、11、12，并真正使用 `stopAcknowledged`。

### 17.1 Entry 生命周期

为每个 `ProcessSupervisor::Entry` 增加明确阶段，例如：

```text
Stopped
StartingProcess
WaitingRegistration
WaitingStarted
Running
StopRequested
WaitingStopAck
WaitingProcessExit
RestartDelay
Failed
```

另加：

- `StopPurpose`：Stop、ManualRestart、AutoRestart、Shutdown；
- 本轮操作 generation；
- 注册、停止和重启 deadline；
- 本轮是否收到 stopAck；
- 是否已经排队后续启动。

删除可以由阶段唯一推导的重复布尔状态，避免同一 Entry 同时出现
`running=true`、`stopping=true` 等矛盾组合。

### 17.2 异步 API

增加：

```text
requestStop(moduleId)
requestRestart(moduleId)
restartFinished(moduleId, success, detail)
operationBusyChanged(moduleId, busy)
startupBatchFinished(errors)
```

这些请求只验证并启动状态转换，必须立即返回。最终成功或失败由 signal 通知。

### 17.3 停止顺序

停止入口必须按以下顺序执行：

1. 在 `outgoingMutex` 下把 `outgoingAccepting=false`、`outgoingStopping=true`。
2. 调用 MessageBus 的单模块停止入口，拒绝投向该模块的新消息。
3. 统计并清空尚未发送的等待项。
4. 设置 `StopRequested/WaitingStopAck` 和独立 stop deadline。
5. 发送 stop 控制帧。
6. 等待 stopAck 或 `QProcess::finished` 信号。
7. 到达 deadline 后只升级该 Entry 的终止阶段，不影响其他模块的 deadline。

### 17.4 删除 GUI 嵌套等待

以下路径不得再调用 `QCoreApplication::processEvents()`、`QThread::msleep()`、
`waitForStarted()` 或 `waitForRunning()`：

- 用户点击重启；
- 自动重启；
- GUI 运行期间的普通 stop；
- supervision tick。

`beginStartEntry()` 调用 `QProcess::start()` 后立即返回，由
`QProcess::started/errorOccurred/finished`、Socket register 和 started 帧推进状态。

应用程序最终退出时可以有一个专用的有界收尾函数，但它必须：

- 先并行向所有子进程发 stop；
- 禁止所有自动/手动重启；
- 使用一个总 deadline，而不是每个进程重新获得完整超时；
- 到点后结束剩余整个 QProcess；
- 不进入可以处理普通重启操作的嵌套事件循环。

### 17.5 UI 接入

1. `MainWindow::onRestartModuleRequested()` 只提交异步请求。
2. `ModuleManagerDialog` 保存 `restartButtons_`。
3. Starting、Stopping、Restarting 期间禁用按钮。
4. 重复点击立即返回“操作正在进行”，不能创建第二轮重启。
5. 状态栏和占位页显示“停止中/重启中”，最终错误通过 signal 统一报告。

### 17.6 本批测试

- `processRestartRequestDoesNotBlockGui`
- `processRejectsDuplicateRestartRequest`
- `processStopClosesIngressImmediately`
- `processDeadlinesAreIndependentPerEntry`
- `processStopAckAdvancesState`
- `processShutdownUsesOneTotalDeadline`
- `processStartupBatchCompletesAsynchronously`

关键断言：

- `requestRestart()` 很快返回，GUI QTimer 在重启期间持续触发；
- 停止请求返回后发布的新消息立即被拒绝；
- A 等待停止时，B 的自动重启不会延长或缩短 A 的 deadline；
- 同一模块没有两轮并发 restart；
- `stopAcknowledged` 被状态机读取，而不是只写不读。

### 17.7 本批文档同步检查点

- 更新 `修改计划.md` 和 `修改进度.md`。
- 同步 `Docs\Guides\操作与调试指南.md` 的手动重启步骤和状态显示。
- 同步 `Docs\Explanation\架构说明.md` 的进程状态机和消息停止顺序。
- 同步 `Docs\Reference\框架配置文件参考.md` 中 Stop/Restart timeout 的准确语义。

### 17.8 实际实施结果

- `ProcessSupervisor::Entry` 已改为明确的 `LifecyclePhase`、`StopPurpose`、generation、
  独立 startup/stop deadline、pending start 和 operation busy；删除旧的
  `registered/running/stopping/faulted` 重复布尔状态。
- `startAll()`、`requestStop()` 和 `requestRestart()` 只提交异步状态转换；
  `QProcess`、`QLocalSocket` 和监督定时器继续推进，最终以 signal 结算。
- 停止顺序已固定为先关闭父到子及 MessageBus 入口，再清等待队列、建立独立
  deadline，最后发送 stop；`stopAcknowledged` 会推进到等待进程退出阶段。
- 普通 stop/restart/tick 已无 `processEvents()`、`msleep()`、`waitForStarted()`、
  `waitForRunning()` 和无界等待。应用退出并行发 stop，并在一个总 deadline 内
  依次升级 `terminate()`/`kill()`，两者都作用于完整 `QProcess`。
- `ModuleManagerDialog` 保存 `restartButtons_`，Starting/Stopping/Restarting 或
  operation busy 时禁用；`MainWindow` 只提交请求，并从 `restartFinished` 显示结果。
- 第 17.6 节 7 项直接测试已实际运行并通过；迁移为异步断言的 8 项既有进程/IPC
  回归也全部通过。命令、结果和剩余门槛记录在 `修改进度.md`。

## 18. 批次 3：有界退出与卡死回调

覆盖问题：4、13。

### 18.1 主进程 MessageBus

1. 删除 `ModuleQueue::finishStop()` 超时后的无参数 `wait()`。
2. 返回强类型结果，例如 `Stopped` 或 `TimedOut`。
3. `MessageBus::stopQueues()` 返回详细报告：

```text
stoppedModuleIds
timedOutModuleIds
```

4. 超时后只清理尚未执行的等待项，不能声称已经停止当前业务回调。
5. `unregisterModule()` 不得 delete 仍在运行的 QThread。
6. `PluginManager` 不得卸载仍有线程执行其代码的 DLL。
7. FrameworkRuntime 只调用一次队列停止流程，避免
   `FrameworkRuntime -> PluginManager -> MessageBus 析构` 重复等待同一队列。

### 18.2 协作停止

在 `ModuleEndpoint` 增加不改变对象布局的线程安全查询，例如：

```cpp
bool isStopRequested() const;
```

它可以在现有 `mutex_` 下根据运行状态返回结果。长期循环或有界轮询的模块应
定期检查并尽快从 `onMessage()` 返回。停止开始时先 `setRunning(false)`，
后续 `publish()` 立即失败。

不把 `onStop()` 与仍在执行的 `onMessage()` 并发调用；如果消息线程超时，
跳过该模块的 `onStop()` 和 DLL 卸载。

### 18.3 子进程消息线程

1. `MessageQueue::stopAndDrain()` 只做有限 `wait(timeout)`。
2. 删除 `QThread::terminate()`。
3. 超时后不 delete MessageQueue、不 delete ModuleEndpoint，也不继续在同一
   子进程内运行其他业务。
4. 尽量发送一条不干净停止诊断，然后结束整个子进程；如果无法发送，由父进程
   在 `StopTimeoutMs` 到期后结束整个 QProcess。

### 18.4 主进程超时策略

主进程 DLL 回调卡死时无法同时做到“安全卸载 DLL”和“继续正常运行”。本计划
采用以下明确策略：

1. 把卡死模块放入 quarantine，不删除线程、对象或 loader。
2. 尽量停止其他可以安全停止的组件。
3. 有界刷新退出诊断。
4. 在框架退出边界调用集中封装的 process-level fail-fast。

该 fail-fast 只用于“应用本来就在退出且 DLL 回调无法停止”的场景，不能用于
普通运行期间的模块重启。

### 18.5 本批测试

- `messageBusStopTimeoutIsBounded`
- `timedOutQueueIsNotDeletedOrUnloaded`
- `processHungCallbackEndsWholeChild`
- `shutdownRequestsFailFastForHungInProcessModule`
- `messageQueueNeverUsesThreadTerminate`

测试进程中不能真的结束 Qt Test 主进程；fail-fast 必须有可替换的测试入口，
测试只记录是否请求了 fail-fast。阻塞回调测试结束前要释放测试条件，使测试
程序不残留线程。

### 18.6 本批文档同步检查点

- 更新 `修改计划.md` 和 `修改进度.md`。
- 同步 `Docs\Reference\模块开发接口参考.md` 的协作停止规则。
- 同步 `Docs\Explanation\架构说明.md` 的有界退出、quarantine 和进程隔离。
- 同步 `Docs\Guides\操作与调试指南.md` 的卡死模块诊断与退出行为。

### 18.7 实际实施结果（2026-08-05）

状态：本批代码、5 项新增直接 Debug x64 Qt Test、13 项相关回归和直接相关文档
已完成；完整 Debug/Release 与真实桌面人工验证仍由第 20/24 章收口。

1. `ModuleQueue::finishStop()` 改为返回 `Stopped/TimedOut`，删除超时后的无参数
   `wait()`；`MessageBus::stopQueues()` 使用一个总 `QDeadlineTimer` 并返回逐模块
   报告。超时时只清除、统计尚未执行的消息，不删除仍在运行的 `QThread`。
2. `PluginManager` 对超时主进程模块执行 quarantine：跳过并发 `onStop()`、注销和
   DLL 卸载。`FrameworkRuntime` 只执行一次总线停止，并在应用本来就在退出的
   边界通过可测试的 `ProcessFailFast` 集中请求进程级 fail-fast。
3. `ProcessRuntime::MessageQueue` 删除 `QThread::terminate()`；回调超时后不删除
   队列或 endpoint，尽量发送 dirty stopAck，然后结束整个子进程。正常路径显式
   `prepareForExit()`，父进程只对完整 `QProcess` 使用 `terminate()/kill()`。
4. 新增直接测试 5 项，结果 `7 passed, 0 failed`（含 init/cleanup，828 ms）；
   MessageBus、插件、跨进程 Latest/Reliable/共享内存和第 17 批状态机 13 项回归
   结果 `15 passed, 0 failed`（含 init/cleanup，13608 ms）。测试均使用 Qt
   `windows` 平台插件。
5. Debug x64 `QFrameworkTests` 串行构建成功；仅有既有
   `windeployqt: VCINSTALLDIR is not set` 非致命环境警告。

## 19. 批次 4：ProcessRuntime 线程安全、控制帧和小问题

覆盖问题：14，以及小问题中的通信、统计、边界和缩进项。

### 19.1 修复 running_ 数据竞争

问题目标是 `ProcessRuntime::running_`，不是已经由 `ModuleEndpoint::mutex_`
保护的 SDK `running_`。

修改原则：

1. `queuePublish()` 不从业务线程读取 `ProcessRuntime::running_`。
2. `queuePublish()` 不从业务线程调用 `socket_->state()`。
3. 业务线程只进入 `PublishQueue::enqueue()`；该队列在自己的 `QMutex` 下
   判断 accepting。
4. 停止、断开和故障先关闭 PublishQueue 闸门。
5. 所有 `QLocalSocket` 状态判断和 write 都只在 ProcessRuntime 所属线程执行。
6. 对直接操作 Socket 的私有函数添加 Qt 线程归属断言。
7. publishQueue 指针的停止和销毁必须与正在进入 enqueue 的生产者同步，
   不能只把 `running_` 换成原子变量。

### 19.2 控制帧写失败

把子进程：

```cpp
void sendFrame(const QJsonObject& frame);
```

改为：

```cpp
bool sendFrame(const QJsonObject& frame);
```

必须检查：

- register；
- startFailed/started；
- windowReady；
- ping/pong；
- deliveryAck/publishAck；
- stopAck；
- 日志控制帧。

发送失败时进入统一 transport fault/finish 流程，不能只依赖之后的心跳超时。

父进程发送 ping 时：

- 成功才更新 `lastPingMs`；
- 失败立即 `handleFault()`；
- 不继续等待 pong timeout。

### 19.3 队列统计

生命周期清理前：

- 尚未写入 Socket 的等待项增加 `dropped`；
- 闸门关闭后到达的新消息增加 `rejected`；
- 已写入但没有 ACK 的消息增加单独的 `abandoned`；
- 每个模块限频记录告警；
- 停止/故障结束时输出一次汇总；
- 同一轮清理多次进入时不能重复计数。

### 19.4 七项小问题处理

| 小问题 | 本轮处理 |
| --- | --- |
| `stopAcknowledged` 只写不读 | 在批次 2 中用于推进 WaitingStopAck -> WaitingProcessExit |
| `ProcessSupervisor::outgoingChanged` 无 wait | 删除变量和全部 wake 调用 |
| `PublishQueue::available_` 无 wait | 删除变量和全部 wake 调用 |
| 心跳发送失败不检查 | 失败立即进入统一故障流程 |
| 子进程 sendFrame 失败不检查 | 改为 bool 并逐处处理 |
| 停止清队列不记账 | 增加 dropped/rejected/abandoned 统计和限频日志 |
| 重启窗口边界使用 `>` | 改为定义明确的 `>=` 并增加边界测试 |
| FrameworkRuntime 缩进错误 | 修正 Logger 启动失败分支的 return 缩进 |

表格最后两行分别对应原清单中的“边界”和“缩进”，因此原清单仍是 7 类问题，
其中两个条件变量属于同一类。

### 19.5 本批测试

- `processPublishGateIsThreadSafeDuringStop`
- `processControlWriteFailureTriggersFaultImmediately`
- `processStopAccountingIncludesDiscardedMessages`
- `processRestartWindowBoundaryIsInclusive`
- `processSocketAccessStaysOnRuntimeThread`

当前 MSVC/Qt 5.15.2 基线不把 ThreadSanitizer 作为硬性门槛。必须通过：

- 多 QThread 并发 publish/stop/restart 压力测试；
- Qt 线程归属断言；
- 无跨线程 QObject/QLocalSocket 警告；
- 重复运行相关测试，排除偶发竞态。

### 19.6 本批文档同步检查点

- 更新 `修改计划.md` 和 `修改进度.md`。
- 同步 `Docs\Explanation\架构说明.md` 的线程归属、控制帧失败和统计。
- 同步 `Docs\Reference\模块开发接口参考.md` 的 publish 线程规则。
- 核查 `Docs\Reference\框架配置文件参考.md` 的 RestartWindow 边界说明。

### 19.7 实际实施结果（2026-08-05）

状态：本批代码、5 项新增直接 Debug x64 Qt Test、5 轮并发重复压力、6 项既有
IPC/停止回归和直接相关文档已完成；完整 Debug/Release 与真实桌面人工验证仍由
第 20/24 章收口。

1. 业务线程的 `queuePublish()` 不再读取 `ProcessRuntime::running_` 或
   `QLocalSocket::state()`，只在指针互斥和 `PublishQueue` 自身 `QMutex` 下入队；
   stop/delete 与正在进入 enqueue 的生产者使用同一把锁同步。Socket 状态和
   `write()` 只留在 Runtime 线程，私有入口增加 Qt 线程归属断言。
2. 子进程 `sendFrame()` 改为 `bool`，register、started/startFailed、windowReady、
   ping/pong、ACK、stopAck 和日志帧均检查写入结果并进入有界退出路径。父进程
   ping 只有发送成功才更新 `lastPingMs`，失败立即 `handleFault()`。
3. 删除 `PublishQueue::available_` 和 `ProcessSupervisor::outgoingChanged` 两个没有
   等待者的条件变量。父、子发送队列分别统计未写入项 `dropped`、闸门/容量拒绝
   `rejected`、已写入未 ACK 项 `abandoned`；同一 generation 只汇总一次。
4. restart 窗口改为 `elapsed >= RestartWindowMs` 时开始新窗口。并发测试发现并
   修复一处停止竞态：停止阶段迟到 publishAck 写失败不再误判手动重启失败。
5. 5 项新增直接测试结果 `7 passed, 0 failed`（含 init/cleanup，1756 ms）；
   并发 publish/stop/restart 与停止记账组合重复 5 轮全部退出码 0；6 项既有
   Process IPC、Latest/Reliable、inline/shared、停止闸门和卡死子进程回归结果
   `8 passed, 0 failed`（含 init/cleanup，9244 ms）。均使用 Qt `windows` 平台。
6. Debug x64 `QFrameworkTests` 串行构建成功；仅有既有
   `windeployqt: VCINSTALLDIR is not set` 非致命环境警告。

## 20. 批次 5：SDK 注释、代码注释和最终文档收口

覆盖问题：15，以及用户要求的注释和全部 Markdown 同步。

### 20.1 修正 onStart 契约

当前实际行为是：

```text
框架先允许 publish
 -> 调用 onStart()
 -> onStart 成功：继续 Running
 -> onStart 失败：撤销发布权限并清理
```

因此 `ModuleEndpoint.h` 应明确写成：

> `onStart()` 执行期间允许调用 `publish()`。如果 `onStart()` 返回 false
> 或抛出异常，框架会立即关闭发布能力并把模块置为启动失败。

同步修正：

- SDK 头文件注释；
- 模块接口参考；
- 生命周期说明；
- 创建模块教程中的启动示例。

### 20.2 代码注释复查

按第 13.3 节检查所有本轮修改文件：

- 新状态枚举和状态转换有中文说明；
- QObject/Socket 线程归属有中文说明；
- pending 显示意图有中文说明；
- 有界等待和 fail-fast 取舍有中文说明；
- 非显而易见的统计含义有中文说明；
- 不做全仓注释重写。

### 20.3 最终收口

1. 检查所有计划问题均有代码和测试对应。
2. 检查所有 Markdown 与最终行为一致。
3. 删除已经不再使用的函数、include 和注释。
4. 运行 Debug、Release、全量 Qt Test 和 `git diff --check`。
5. 记录人工 Dock 验证、未执行硬件验证和任何环境警告。

### 20.4 实际实施结果（2026-08-05）

状态：第 12～20 章代码、直接测试、完整 Debug/Release 和全部 Markdown 审计已
完成；第 24.4 节真实桌面人工操作因当前工具会话没有可见窗口站而保留为外部门槛，
没有用 offscreen 或仅进程存活替代人工通过。

1. SDK 头文件、接口参考、架构说明、创建模块教程和开发计划统一为：框架先开放
   当前模块发布入口，再调用 `onStart()`；返回 `false` 或抛异常会立即撤销权限。
2. 最终 diff 复核发现并修正一处代码漂移：`windowReady` 原先仍无条件发送
   `showWindow`。现在只有 `requestedDockVisibility_ && uiAvailable_` 成立才发送；
   布局明确隐藏时只嵌入 HWND，不补显示，之后用户勾选仍可补发显示命令。
3. 完成性审计又补充真实子进程测试
   `processUiLateWindowHonorsLayoutVisibility`：先在 ProcessUi 未 ready 时加载显示
   布局，确认 `windowReady` 后补显示；再加载明确隐藏布局并异步重启，确认新窗口
   只嵌入不显示，之后菜单勾选仍能显示。补强后的 10 项直接 Debug x64 测试结果
   `12 passed, 0 failed`（含 init/cleanup，12893 ms）。
4. 最终 Debug x64 `QFrameworkTests` 构建成功，完整测试结果
   `48 passed, 0 failed, 0 skipped`（24768 ms）。
5. 最终 Release x64 全解决方案构建成功，完整测试结果
   `48 passed, 0 failed, 0 skipped`（20904 ms）。构建仅保留既有
   `windeployqt: VCINSTALLDIR is not set` 非致命环境警告。
6. 禁用项审计确认：消息线程没有 `QThread::terminate()`、没有
   `Qt::BlockingQueuedConnection`、没有 `QCoreApplication::processEvents()`，也未新增
   `std::thread/std::mutex`。源码仍有 Logger Worker 的既有 `terminate()` 兜底，
   不属于本轮消息线程范围；完整子进程只使用 `QProcess::terminate()/kill()`。
7. 桌面验收连续三轮排查结果一致。最终一轮在 active console Session 1 显式设置
   `QT_QPA_PLATFORM=windows` 并可见启动 Release 主进程；主进程及两个子进程均
   `Responding=True`，但 `MainWindowHandle=0`，`computer-use` 的 `list_windows()`
   仍报 `EnumWindows 0x80070003`，独立 User32 `EnumWindows` 返回
   `WindowCount=0`。三个已核对工作区路径的测试进程均已清理，需在真正可交互的
   Windows 窗口站按第 24.4 节继续人工验收。

### 20.5 用户桌面复现后的 ProcessUi 尺寸加固（2026-08-05）

用户完成第 24.4 节原菜单、标签、布局和异步重启操作后，继续复现出新的嵌入问题：
子进程内容在 Dock 右侧、底部留下宿主背景；主窗口连续放大缩小时偶发旧窗口失效，
重新加载后 Dock/主窗口被异常拉长。代码漂移核对确认原实现只在显示时发送一次
`showWindow(width, height)`，Dock 后续 resize 没有任何父子尺寸同步，且重启占位阶段
同步删除外部 `QWindow` 容器。

本批采用最小 Qt 5.15.2 修复：

1. `ProcessWindowHost` 使用 50 ms 单次 `QTimer` 合并 `resizeEvent/showEvent`，容器设置
   `QSizePolicy::Ignored` 和零最小尺寸，宿主提供稳定 size hint，避免 foreign window
   反向放大 Dock 约束。
2. 父子协议新增独立 `resizeWindow` 控制帧；`showWindow` 只改变可见状态，子进程
   收到 resize 时只调用 `QWidget::resize()`，不重复 `show()`。
3. `MainWindow` 仅在 requested-visible、window ready、Supervisor `Running`、Dock 可见
   同时成立时转发尺寸；明确隐藏和重启占位阶段丢弃迟到尺寸。
4. 旧容器先隐藏并从布局移除，再 `deleteLater()`；不在 HWND 仍处理原生事件时同步
   销毁包装容器。
5. 真实 `ProcessUiExample` 测试已覆盖首次嵌入、连续 resize、重启后新 HWND、隐藏
   状态 resize 不补显示和布局最小尺寸不异常放大。Debug 直接相关测试当前结果
   `9 passed, 0 failed`；完整 Debug、Release 与最终差异审查尚待本批后续执行，未提前
   标记为通过。

### 20.6 ProcessNonUi 重启后的超长日志布局加固（2026-08-05）

用户补充了稳定复现路径：InProcessUi 与 ProcessUi 左右停靠后，手动重启
`ProcessNonUiExample`，主窗口会被拉长且不能恢复。日志显示只有 ProcessNonUi
重启，ProcessUi 没有崩溃或重启；代码核对确认其 `onStart()` 每次发布含 2048 个 `X`
的共享内存演示消息，两个 UI 示例都把完整文本直接交给 `QLabel::setText()`。
因此 QLabel 的 `sizeHint/minimumSizeHint` 将任意长业务文本变成 Dock 和外部 HWND 的
最小宽度约束，旧实现复现为主进程 `12466` 宽、ProcessUi `12482x320`。

本批最小修复：

1. 保留 ProcessNonUi 的完整 2048 字符发布，继续覆盖共享内存和 ACK；InProcessUi、
   ProcessUi 仅对屏幕显示做 160 字符预览，并设置纯文本、换行、零最小宽度和
   `QSizePolicy::Ignored`，不改变消息计数或业务回调。
2. `pluginLoaderAndModuleIntegration` 直接注入同样的超长日志并断言 UI 最小宽度；
   `processIpcAndSupervision` 在 ProcessUi 保持嵌入时手动重启 ProcessNonUi，断言
   ProcessUi HWND 数量、运行状态和客户区尺寸不变。
3. 修复后的直接 Debug 回归为 `4 passed, 0 failed`；随后完整 Debug 与 Release
   均为 `48 passed, 0 failed, 0 skipped, 0 blacklisted`，Release 全解决方案构建成功。
   最终差异审查已进入收口，真实桌面复验仍需按第 24.4 节由用户确认，未用自动化进程
   存活替代可见窗口操作。

## 21. 分批提交和回滚点

建议至少形成以下独立提交：

1. Dock 显示意图、布局 pending 和测试。
2. ProcessSupervisor 异步停止/重启状态机和测试。
3. MessageBus/ProcessRuntime 有界关闭与无 terminate 策略。
4. ProcessRuntime 线程安全、控制帧、统计和小问题。
5. SDK 注释、全部 Markdown 收口和最终验证记录。

每批开始前记录 `git status` 和现有 diff；每批结束后：

- 审查本批 diff；
- 运行本批直接相关测试；
- 更新本批相关 Markdown；
- 在 `修改进度.md` 记录实际命令和结果；
- 通过后才进入下一批。

回滚必须按提交或完整批次进行，不能只替换某个 DLL。主进程和子进程协议发生
变化时，必须保证它们来自同一构建。

## 22. 全部 Markdown 同步要求

用户要求修改过程中和结束后同步所有 `.md` 文档。实施会话必须先运行：

```powershell
git -C D:\QFrameworkv2 ls-files "*.md"
```

当前已知受版本控制 Markdown：

| 文档 | 需要同步的内容 |
| --- | --- |
| `修改计划.md` | 每批状态、实际范围、偏差、测试结果和最终完成状态 |
| `修改进度.md` | 每批追加代码、测试、构建、文档和剩余人工门槛 |
| `开发计划.md` | 更新对应里程碑、完成项和剩余技术债 |
| `Docs\Explanation\架构说明.md` | 显示意图、异步进程状态机、有界退出、线程归属和统计 |
| `Docs\Guides\操作与调试指南.md` | 菜单显示/隐藏、标签布局、异步重启和故障处理 |
| `Docs\Reference\模块开发接口参考.md` | onStart、协作停止、publish 和线程契约 |
| `Docs\Reference\框架配置文件参考.md` | Stop/Restart/Shutdown 参数的准确语义和边界 |
| `Docs\Tutorials\创建四类模块教程.md` | 初学者可理解的生命周期、停止检查和 onStart 发布示例 |
| `Docs\文档索引.md` | 核查链接和各文档描述是否仍准确 |

“同步所有 Markdown”的执行规则：

1. 每批代码完成后立刻更新受影响文档，不把全部文档推迟到最后。
2. 未受本批影响的文档也要核查，并在 `修改进度.md` 的文档审计表中记录
   “已核查，无需修改”，不能为了制造 diff 改时间戳或无关文字。
3. 文档不能提前写“测试通过”；只有实际命令成功后才能记录。
4. 最终重新枚举全部 Markdown，检查没有新增文档被遗漏。
5. 使用 `rg` 搜索旧术语，例如同步 restart、`isVisible()` 表示用户意图、
   `onStart()` 前禁止 publish、`QThread::terminate()` 等，清理过时说明。
6. 检查相对链接、文件名、UTF-8、末尾换行和 `git diff --check`。

## 23. 测试和验收矩阵

### 23.1 Dock 和布局

- 菜单勾选显示、取消勾选隐藏。
- Dock 关闭按钮取消显示意图。
- 标签切换不改变菜单勾选。
- 两个标签保存/加载后仍属于同一标签组。
- 布局要求显示但窗口未 ready 时进入 pending。
- ready 后自动补显示，明确隐藏时不补显示。
- 加载失败完整回滚。

### 23.2 进程生命周期

- 手动重启不阻塞 GUI。
- 重启期间按钮防重复。
- 停止开始后拒绝新消息。
- A 停止与 B 自动重启 deadline 独立。
- 自动、手动和 shutdown 重启意图不冲突。
- 所有新增等待有 timeout，停止/断线/故障/重启能唤醒。

### 23.3 卡死和线程安全

- 卡死回调不会进入无参数 wait。
- 消息线程不调用 `QThread::terminate()`。
- 超时线程不被 delete，其 DLL 不被 unload。
- 子进程卡死时结束整个 QProcess。
- 多线程 publish 与 stop/restart 压力测试稳定。
- 业务线程不直接访问 QLocalSocket。

### 23.4 控制帧和小问题

- ping、register、ACK、started、windowReady 写失败进入明确故障路径。
- dropped/rejected/abandoned 计数正确且日志限频。
- restart 窗口 `elapsed == RestartWindowMs` 的边界行为明确。
- 两个无等待者条件变量和错误缩进被清理。

### 23.5 SDK 和文档

- `onStart()` 注释与主进程、子进程实际行为一致。
- 新增中文注释解释关键 Qt 状态和线程边界。
- 全部 Markdown 完成更新或“已核查无需修改”记录。

## 24. 构建、测试和人工验证

### 24.1 Debug x64

```powershell
& "D:\Program Files (x86)\Microsoft Visual Studio\2017\Community\MSBuild\15.0\Bin\MSBuild.exe" D:\QFrameworkv2\QFramework.sln /m /p:Configuration=Debug /p:Platform=x64 /p:QtInstallDir=D:\Qt\5.15.2\msvc2019_64 /v:minimal
```

先运行新增和直接相关测试，再运行完整套件：

```powershell
D:\QFrameworkv2\Bin\v141\x64\Debug\QFrameworkTests.exe
```

### 24.2 Release x64

Debug 全部通过后执行：

```powershell
& "D:\Program Files (x86)\Microsoft Visual Studio\2017\Community\MSBuild\15.0\Bin\MSBuild.exe" D:\QFrameworkv2\QFramework.sln /m /p:Configuration=Release /p:Platform=x64 /p:QtInstallDir=D:\Qt\5.15.2\msvc2019_64 /v:minimal

D:\QFrameworkv2\Bin\v141\x64\Release\QFrameworkTests.exe
```

### 24.3 静态检查

```powershell
git -C D:\QFrameworkv2 diff --check
git -C D:\QFrameworkv2 status --short
```

并检查：

```powershell
rg -n "QThread::terminate|[^A-Za-z]terminate\(\)|QCoreApplication::processEvents|wait\(\)" Source\QFramework Source\QFrameworkSdk
```

搜索结果需要人工分类：

- `QProcess::terminate()`/`kill()` 是完整子进程边界，可按策略保留；
- Logger Worker 的既有 `QThread::terminate()` 不属于本轮消息线程问题，但最终
  报告必须明确仍然存在，不能误报“全仓已无 terminate”；
- MessageBus 无参数 wait 和 ProcessRuntime MessageQueue terminate 必须消失。

### 24.4 人工 UI 验证

在真实 Windows 桌面完成：

1. 显示两个模块 Dock，并叠成标签。
2. 来回切换标签，观察两个菜单项。
3. 保存布局、打乱位置、重新加载。
4. 重启一个 ProcessUi，观察占位页、pending 和窗口恢复。
5. 重启期间拖动主窗口、打开菜单，确认 GUI 可响应。
6. 对布局中明确隐藏的 ProcessUi 重启，确认不会自动弹出。

Dock offscreen 测试过去出现过挂起，不能把 offscreen 超时当作通过。自动测试
通过后仍必须保留这组人工验收。

## 25. 新会话实施提示词

下面内容可以直接复制到新会话：

```text
请在 D:\QFrameworkv2 中直接完成代码修改。这是代码实施任务，不是重新分析需求，也不是再次只写计划。

开始前：
1. 完整阅读 D:\QFrameworkv2\修改计划.md。
2. 第 1～11 章是已经完成的历史 IPC/日志计划，不要重复实施；本次严格执行第 12～25 章。
3. 查找并遵守仓库中的 AGENTS.md；检查 git status 和现有 diff。当前或之后出现的用户改动、未跟踪文件都要保留，不得回滚或覆盖。
4. 重新核对目标函数与计划是否有代码漂移。如有漂移，先说明证据并做最小调整，但继续完成实施，不要退回只给建议。
5. 用 git ls-files "*.md" 枚举全部 Markdown，建立文档同步清单。

本次必须完成：
1. 问题 2：菜单勾选显示、取消勾选隐藏。
2. 问题 3：标签布局保存/加载后不拆散。
3. 问题 4：删除 MessageBus 超时后的无限 wait，并提供安全、有界的卡死处理。
4. 问题 7：布局要求显示但子进程窗口晚到时，windowReady 后自动补显示；布局明确隐藏时不能补显示。
5. 问题 8：手动重启改为异步状态机，不阻塞主窗口。
6. 问题 9：切换标签不能取消菜单勾选。
7. 问题 11：停止开始时先关闭消息入口，再发送 stop。
8. 问题 12：A 的停止等待不能通过 processEvents 处理 B 的同步重启；每个模块使用独立 deadline。
9. 问题 13：删除 ProcessRuntime 消息线程的 QThread::terminate()；子进程卡死时结束整个 QProcess，不能强杀一条线程后继续运行。
10. 问题 14：修复 ProcessRuntime::running_ 数据竞争，业务线程不能读取 QLocalSocket 状态；Socket 操作只在其 QObject 所属线程执行。
11. 问题 15：修正 SDK onStart() 注释，明确 onStart 期间允许 publish，失败后撤销发布权限。
12. 完成修改建议.txt 中“🔧 小毛病”的全部 7 类：真正使用 stopAcknowledged、删除两个无等待者的 QWaitCondition、检查心跳和子进程控制帧发送、停止清队列记账、restart 窗口使用 >=、修正 FrameworkRuntime 缩进。

Qt 和代码可读性要求：
1. 我长时间没有使用 C++，请尽量使用 Qt 5.15.2 的写法：QHash、QQueue、QSet、QMutex、QMutexLocker、QWaitCondition、QTimer、QElapsedTimer、QDeadlineTimer、QAction、QSignalBlocker、signal/slot、QProcess、QLocalSocket、QSharedMemory 和 QJsonObject。
2. 不要引入 std::thread、std::mutex、协程、复杂模板、无锁队列或新第三方依赖。
3. 不使用 Qt::BlockingQueuedConnection。
4. 所有新增等待必须有超时，并能在停止、断开、故障和重启时结束或被唤醒。
5. 为关键状态、线程归属、停止闸门、deadline、pending 布局和 fail-fast 取舍添加简明中文注释；不要给显而易见代码逐行加注释，也不要做全仓注释重写。
6. 保持小范围、可审查的 diff；优先修改现有 h/cpp，不做无关重构。
7. Latest 仍然只覆盖同主题最旧等待帧，Reliable 绝不覆盖；不要破坏已经完成的跨进程有界队列和 100 ms 日志刷新。

重要安全约束：
1. 不允许用 QThread::terminate() 结束消息线程。
2. MessageBus 的 onMessage 回调超时后，不能 delete 仍在运行的 QThread，也不能卸载仍被该线程执行的 DLL。
3. 主进程 DLL 回调卡死时按计划 quarantine，并在应用退出边界执行有界 process-level fail-fast；普通运行期间不能用该策略重启模块。
4. QProcess::terminate()/kill() 是完整子进程隔离边界，要和 QThread::terminate() 明确区分。
5. 停止入口先关闭父到子接收闸门并记账，再发送 stop。

实施和文档同步要求：
1. 按修改计划.md第 16～20 章分批实施，每批完成代码、直接相关测试和文档后再进入下一批。
2. 修改过程中就同步相关 Markdown，不要等所有代码完成后才补文档。
3. 每批更新 修改计划.md 的状态，并在 修改进度.md 追加实际文件、命令、结果、失败和剩余门槛。
4. 同步或核查全部受版本控制 Markdown：修改计划.md、修改进度.md、开发计划.md、Docs/Explanation/架构说明.md、Docs/Guides/操作与调试指南.md、Docs/Reference/模块开发接口参考.md、Docs/Reference/框架配置文件参考.md、Docs/Tutorials/创建四类模块教程.md、Docs/文档索引.md。
5. 未受影响文档也要在最终文档审计表中记录“已核查，无需修改”，不要为了制造 diff 改无关文字。
6. 文档只有在实际测试成功后才能写“已通过”。

测试和完成要求：
1. 增加修改计划.md第 16～19 章列出的 Dock、布局、异步重启、停止闸门、独立 deadline、卡死回调、线程安全、控制帧失败、统计和边界测试。
2. 先运行最快的直接相关 Debug x64 测试，再运行完整 Debug x64 QFrameworkTests。
3. Debug 全部通过后运行 Release x64 构建和完整测试。
4. 运行 git diff --check，并审查最终 git diff 和 git status。
5. 在真实 Windows 桌面人工验证菜单显示/隐藏、标签切换、布局保存/加载、ProcessUi 晚到和重启期间 GUI 响应；offscreen Dock 超时不能算通过。
6. 最终报告用容易理解的中文说明：完成了什么、关键 Qt 数据流、修改文件、测试/构建结果、全部 Markdown 同步结果、仍未执行的人工/硬件门槛。

范围限制：
1. 本次不自动实施问题 16 和“界面设计不合理”章节。
2. 不迁移 VS/Qt/工程体系，不新增遥测、网络调用或第三方依赖。
3. 不回退已有 Latest/Reliable、ACK、共享内存和日志 100 ms 实现。

不要只给建议，也不要在中途完成一两个文件后停止。请持续实施、同步文档、构建和测试，直到第 12～25 章范围全部完成，或者出现经过至少三轮安全排查仍无法绕过的真实外部阻塞。
```
