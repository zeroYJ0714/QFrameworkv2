# QFramework 无边框窗口与 TechDashboard 交互修复计划

> 日期：2026-08-05
> 文档类型：代码修改执行计划
> 目标读者：维护 QFramework 的 Qt/C++ 开发者
> 工作区：`D:\QFrameworkv2`

## 1. 文档状态

- 状态：代码已实施，聚焦 Debug QtTest 和完整 Debug 已通过；Release 重建被 QtMsBuild
  `CriticalSection` 事件锁访问错误阻塞，真实桌面人工 UI 验收仍在收口。
- 本文件同时保留原计划和实际实施记录；“自动测试通过”不等于真实鼠标拖动、贴边、
  多显示器和 DPI 验收已经通过。
- `修改计划.md` 在本次写入前是空文件。本计划只记录当前 UI 修改，不恢复 Git
  基线中已经被清空的旧 IPC、日志和进程生命周期历史计划。
- 实施前必须检查 `git status` 和目标文件现有 diff，保留用户已经完成的修改，
  不回滚、不覆盖、不顺带整理无关代码。

## 2. 本次目标

本次把以下三个问题作为同一批主窗口交互改造处理：

1. 使用 `TechDashboard.qss` 后，顶部菜单悬浮时文字和背景对比不足，文字难以辨认。
2. 两个 `QDockWidget` 左右或上下停靠后，中间分隔条不明显、命中区域难找，鼠标
   难以拖动调整两个界面的大小。
3. 移除 Windows 原生白色标题栏，改为和科技主题一致的无边框窗口：
   - 左侧直接放置现有“文件、模块、样式”菜单，不显示额外图标或 `QFramework` 文字；
   - 右侧依次放置最小化、最大化/还原、关闭按钮；
   - 保留窗口拖动、边缘缩放、双击最大化、任务栏工作区和贴边停靠等正常窗口行为。

目标界面结构：

```text
+--------------------------------------------------------------------------------+
| [文件(F)] [模块(M)] [样式(S)]                          [最小化][最大化][关闭] |
+--------------------------------------------------------------------------------+
|                         上方或左侧 QDockWidget                                |
+======================== 可悬浮、可拖动分隔条 ================================+
|                         下方或右侧 QDockWidget                                |
+--------------------------------------------------------------------------------+
|                                                        状态栏：运行中 4 / 4 |
+--------------------------------------------------------------------------------+
```

## 3. 已确认的原因

### 3.1 菜单悬浮文字难以辨认

当前 `config\Styles\TechDashboard.qss` 已定义：

- `QMenu`；
- `QMenu::item`；
- `QMenu::item:selected`。

但它没有定义 `QMenuBar` 和 `QMenuBar::item:selected`。`QMenu` 是点击菜单后弹出的
下拉菜单，`QMenuBar` 是窗口顶部的“文件、模块、样式”这一行，两者不是同一个
Qt 控件。

因此鼠标悬浮顶部菜单时，Qt/Windows 仍可能使用系统默认悬浮背景，而文字颜色继续
继承科技主题的浅色文字，形成“浅色文字叠在浅色背景上”的低对比状态。

结论：必须同时给 `QMenuBar` 的正常、悬浮、按下和禁用状态明确设置文字色和背景色，
不能只依赖 `QMenu::item:selected`。

### 3.2 Dock 中间分隔条难以拖动

当前 QSS 只定义了：

```css
QSplitter::handle { ... }
QSplitter::handle:hover { ... }
```

但主窗口中的模块界面由 `QMainWindow` 和 `QDockWidget` 管理，两个 Dock 之间的调整
手柄不是普通 `QSplitter::handle`。Qt 5.15.2 对该区域使用
`QMainWindow::separator`。

当前源码已经满足以下前提：

- `MainWindow` 调用了 `setDockNestingEnabled(true)`；
- `ManagedDockWidget` 保留 `QDockWidget::DockWidgetMovable`；
- Dock 允许 `Qt::AllDockWidgetAreas`；
- 每个 Dock 的最小尺寸是 `180 x 120`。

所以本次首先修复分隔条的控件选择器、可见性和命中尺寸，不修改 Dock 的显示意图、
标签关系、布局保存语义或最小尺寸。拖动到 `180 x 120` 下限后不能继续缩小属于预期
保护，不应误判为分隔条失效。

### 3.3 最上方白色区域不能由 QSS 直接修改

当前 `StyleManager` 最终调用 `QApplication::setStyleSheet()`。QSS 只能绘制 Qt 客户区
控件，截图中最上面的窗口标题、最小化、最大化和关闭按钮属于 Windows 非客户区，
由操作系统绘制。

因此仅继续补充 QSS 不能消除白色标题栏。既然目标已经确定为无边框窗口，应使用
`Qt::FramelessWindowHint` 移除原生标题栏，再由 Qt Widgets 绘制新的标题栏。

## 4. 行为约束

### 4.1 必须保留的现有功能

- “文件”菜单中的加载布局、保存当前布局、布局另存为和退出功能保持不变。
- “模块”菜单中的模块管理、模块勾选、显示意图和 late-ready 行为保持不变。
- “样式”菜单中的选择 QSS 和重新加载当前 QSS 功能保持不变。
- 菜单仍保留 `&F`、`&M`、`&S` 助记键语义。
- 关闭按钮必须调用 `MainWindow::close()`，不能绕过现有 Qt 关闭事件和
  `FrameworkRuntime` 的退出清理流程。
- 最大化后按钮图标和提示文字切换为“还原”；恢复普通窗口后再切回“最大化”。
- Dock 的对象名、标签关系、`saveState()/restoreState()` 和 `.qflayout` 格式保持不变。
- ProcessUi 的 HWND 嵌入、尺寸同步和重启恢复逻辑保持不变。

### 4.2 无边框窗口必须补回的系统行为

移除原生标题栏以后，以下行为不能丢失：

- 在自绘标题栏空白区域按住鼠标可以移动窗口；
- 双击标题栏空白区域可以最大化或还原；
- 普通状态下四条边和四个角都可以缩放；
- 最大化状态下禁用边缘缩放命中；
- 最大化窗口不能覆盖 Windows 任务栏；
- 从最大化状态拖动标题栏能够恢复并继续移动；
- 拖到屏幕左、右或顶部仍能使用 Windows 贴边布局；
- 菜单、标题栏按钮和其他可点击子控件不能被误判为窗口拖动区域；
- `Alt+F4` 必须继续关闭窗口；应验证 `Alt+Space` 系统菜单是否仍可用；
- 多显示器和不同缩放比例下不能出现跳动、错位或无法缩放。

不要使用持续执行 `QWidget::move()` 的手写鼠标移动循环。Windows 下应优先通过
原生命中测试交给系统完成移动和缩放，以保留贴边停靠、多显示器和 DPI 行为。

### 4.3 Qt 优先和初学者可读性要求

用户已经明确说明长时间没有使用 C++，因此本批代码必须优先使用 Qt 5.15.2 的类型、
事件和对象模型，不能为了“现代 C++”形式引入难以理解的模板或标准库并发写法。

优先使用：

- `QString`、`QByteArray`、`QPoint`、`QRect`、`QSize`；
- `QWidget`、`QMainWindow`、`QMenuBar`、`QToolButton`、`QHBoxLayout`；
- `QEvent`、`QCloseEvent`、`QWindowStateChangeEvent`；
- QObject 父子关系管理控件所有权；
- Qt signal/slot 表达最小化、最大化/还原和关闭请求；
- `QStyle::StandardPixmap` 获取窗口控制图标；
- `QPointer` 只在对象可能被异步删除且确实需要防悬空时使用，不机械添加。

尽量避免：

- `std::thread`、`std::mutex`、协程和无锁结构；
- 复杂模板、模板元编程和为本批功能新增泛型框架；
- 多层嵌套 lambda、难以追踪的回调链和不必要的函数式写法；
- 用裸 `new/delete` 手工管理本可由 Qt 父子关系管理的控件；
- 把 Win32 `HWND`、`MSG`、`WPARAM`、`LPARAM` 扩散到 `WindowTitleBar`、模块或 SDK。

Windows 无边框行为无法完全用 QSS 或普通 QWidget API 实现，因此
`MainWindow::nativeEvent()` 中允许使用必要的 Win32 消息和常量，但必须做到：

1. 使用 `#ifdef Q_OS_WIN` 把平台代码限制在很小的区域。
2. 将坐标尽早转换成 Qt 容易理解的 `QPoint/QRect`，后续判断优先使用 Qt 类型。
3. 把“角、边、标题栏空白、交互控件”命中判断拆成有明确名称的小函数，避免一个
   超长 `nativeEvent()`。
4. 对 `WM_NCHITTEST`、`HTCLIENT`、`HTCAPTION`、八个边角返回值和
   `WM_GETMINMAXINFO` 写中文解释，说明 Windows 收到返回值后会做什么。
5. 不使用难以审查的位运算技巧压缩命中逻辑，宁可保留清晰的顺序判断。

### 4.4 中文注释要求

本批新增和修改代码必须包含详细、完整、面向久未使用 C++ 的维护者的中文注释。
注释重点不是翻译语法，而是解释“为什么这样做、Qt 对象之间是什么关系、事件从哪里
来、处理后去哪里”。

必须注释：

- `WindowTitleBar` 的职责边界，以及它为什么只发 signal、不直接控制模块和进程；
- 标题栏内部控件的 Qt 父子所有权，说明谁负责自动释放；
- 现有 `QAction` 如何从 `MainWindow` 迁移到新 `QMenuBar`，以及为什么不能复制一套；
- 最小化、最大化/还原、关闭 signal 到 `MainWindow` 槽函数的数据流；
- `QEvent::WindowStateChange` 为什么需要同步最大化/还原图标；
- `Qt::FramelessWindowHint` 移除了哪些系统能力，以及代码补回了哪些能力；
- `WM_NCHITTEST` 的判断顺序，为什么边角必须先于标题栏；
- 为什么菜单和窗口按钮必须返回 `HTCLIENT`，标题栏空白区域才返回 `HTCAPTION`；
- Qt 逻辑坐标、Windows 屏幕坐标和 DPI 缩放之间的转换；
- 最大化时为什么使用 monitor 工作区而不是完整屏幕区域；
- `QMainWindow::separator` 与 `QSplitter::handle` 的区别；
- Dock 最小尺寸导致的正常拖动下限；
- Windows 专用代码的非 Windows 回退行为；
- 自动测试能证明什么，以及真实桌面鼠标验收为什么仍然需要执行。

不需要给 `setText()`、`setIcon()`、简单赋值或显而易见的空指针判断逐行写“设置文本”
一类无信息注释。每个较复杂函数前应先有一段流程说明，函数内部再在关键分支前添加
原因注释，让读者可以先理解数据流，再阅读具体 Qt API。

变量和函数名称必须表达业务含义，例如：

- `isPointInResizeBorder()`；
- `isInteractiveTitleBarChild()`；
- `updateWindowControlState()`；
- `toggleMaximizedState()`。

不要使用 `tmp`、`flag1`、`handleIt` 等需要猜测含义的名称。注释、命名和实现必须
一致，后续行为改变时同步更新注释，不能保留已经过时的说明。

### 4.5 明确不做的内容

- 不修改模块、IPC、消息总线、日志或进程监督逻辑。
- 不修改 `LayoutManager` 的格式版本和 Dock 布局持久化结构。
- 不修改 `ManagedDockWidget` 的 floatable 策略。
- 不降低 Dock 当前 `180 x 120` 最小尺寸。
- 不引入第三方图标库；窗口按钮优先使用 Qt `QStyle::StandardPixmap`。
- 不重做所有 Dock 标题栏和业务模块内部界面。
- 本批保证拖动标题栏触发 Windows 贴边布局；Windows 11 最大化按钮悬浮时显示
  Snap Layout 面板不作为首批强制验收项，不能在未实现时宣称支持。

## 5. 预计修改文件

### 5.1 现有文件

- `config\Styles\TechDashboard.qss`
  - 补充顶部菜单、无边框标题栏和窗口按钮样式；
  - 补充 `QMainWindow::separator`。
- `Source\QFramework\Ui\MainWindow.h`
  - 声明自绘标题栏成员；
  - 声明窗口状态更新和 Windows 原生事件入口。
- `Source\QFramework\Ui\MainWindow.cpp`
  - 启用无边框窗口；
  - 将现有菜单创建到新标题栏左侧；
  - 连接三个窗口按钮；
  - 处理 Windows 命中测试和最大化工作区。
- `Source\QFramework\QFramework.vcxproj`
  - 登记计划新增的标题栏 `.h/.cpp`；
  - 只有实际使用额外 Windows 库时才增加链接项，不预先添加无用依赖。
- `Tests\QFrameworkTests\tst_Baseline.h`
- `Tests\QFrameworkTests\tst_Baseline.cpp`
  - 增加标题栏、菜单、窗口按钮和分隔条的直接测试。

### 5.2 计划新增文件

- `Source\QFramework\Ui\WindowTitleBar.h`
- `Source\QFramework\Ui\WindowTitleBar.cpp`

`WindowTitleBar` 只负责可见标题栏控件和用户操作信号，不负责模块生命周期、布局文件
或 Windows 消息处理。Windows 原生消息仍由顶层 `MainWindow` 处理，避免普通子控件
知道 HWND 和 Win32 细节。

## 6. 第一批：修复 TechDashboard 菜单可读性

### 6.1 增加 QMenuBar 完整状态

在 `TechDashboard.qss` 的菜单区域增加：

```css
QMenuBar { ... }
QMenuBar::item { ... }
QMenuBar::item:hover { ... }
QMenuBar::item:selected { ... }
QMenuBar::item:pressed { ... }
QMenuBar::item:disabled { ... }
```

推荐沿用当前科技主题色：

- 普通背景：`#111c20`；
- 普通文字：`#c4d3d5`；
- 悬浮/展开背景：`#1b5c5d`；
- 悬浮/展开文字：`#f2ffff`；
- 禁用文字使用单独的低强调颜色，但仍要能在 `#111c20` 上辨认。

`selected` 和 `pressed` 必须同时设置 `color` 和 `background-color`，不能只改其中一个。
原有 `QMenu::item:selected` 继续保留，它负责下拉菜单，不与本批规则互相替代。

### 6.2 菜单验收

- 普通、悬浮、菜单展开、禁用四种状态文字均可辨认。
- “文件、模块、样式”三项使用一致规则。
- 下拉 `QMenu` 的选中状态不发生回退。
- 通过键盘 Alt 助记键打开菜单时，选中状态与鼠标打开一致。

## 7. 第二批：修复 Dock 分隔条

### 7.1 使用正确的 Qt 选择器

在 `TechDashboard.qss` 中增加：

```css
QMainWindow::separator {
    width: 6px;
    height: 6px;
    background-color: #26383c;
}

QMainWindow::separator:hover {
    background-color: #4b8c8c;
}
```

说明：

- `width` 用于左右排列 Dock 之间的竖向分隔条；
- `height` 用于上下排列 Dock 之间的横向分隔条；
- 普通状态需要看得见，但不能抢过业务内容；
- 悬浮状态必须明显变化，让用户知道鼠标已经命中；
- 原有 `QSplitter::handle` 继续保留，供真正的 `QSplitter` 使用。

### 7.2 Dock 分隔验收

- 两个 InProcessUi Dock 左右排列时可以拖动中间分隔条。
- 两个 InProcessUi Dock 上下排列时可以拖动中间分隔条。
- InProcessUi 和 ProcessUi 混合排列时同样可以拖动。
- 鼠标进入分隔区域时显示正确的水平或垂直调整光标。
- 拖动过程中分隔条高亮，释放后恢复普通颜色。
- Dock 只能缩小到现有最小尺寸，不能被压成零尺寸。
- 调整后保存并重新加载布局，比例和 Dock 关系仍由 Qt 状态恢复。

### 7.3 条件式二级排查

如果增加 `QMainWindow::separator` 后：

- InProcessUi 之间可以拖动；
- 只有包含 ProcessUi 的分隔条仍不能拖动；

才进一步检查 `ProcessWindowHost` 创建的原生子窗口是否越过宿主 `rect()`、覆盖了分隔条
或截获了鼠标。该分支必须先用真实桌面复现和边界坐标证明，再修改
`ProcessWindowHost.cpp`，不能把嵌入窗口代码作为首轮猜测性修改。

## 8. 第三批：新增自绘标题栏

### 8.1 WindowTitleBar 结构

`WindowTitleBar` 建议包含：

- `QMenuBar`：承载现有“文件、模块、样式”菜单；
- 可伸展空白区域；
- `QToolButton`：最小化；
- `QToolButton`：最大化/还原；
- `QToolButton`：关闭。

建议尺寸：

- 标题栏逻辑高度约 `38px`；
- 每个窗口按钮宽度约 `46px`；
- 使用布局固定高度和按钮槽位，悬浮、图标变化或长文本不能引起标题栏跳动。

建议设置稳定对象名：

- `QFrameworkTitleBar`；
- `QFrameworkTitleMenuBar`；
- `WindowMinimizeButton`；
- `WindowMaximizeButton`；
- `WindowCloseButton`。

标题栏对外只发送清晰信号，例如：

- `minimizeRequested()`；
- `maximizeRestoreRequested()`；
- `closeRequested()`。

### 8.2 复用现有菜单

`MainWindow::createActions()` 当前直接调用三次 `menuBar()->addMenu()`。实施时改为：

1. 构造 `WindowTitleBar`；
2. 调用 `setMenuWidget(titleBar_)`，让标题栏占据 `QMainWindow` 原菜单栏位置；
3. 从 `titleBar_->menuBar()` 取得菜单栏；
4. 把原有三个 `addMenu()` 改为向该菜单栏添加；
5. 原有 `QAction`、对象名、动态属性、signal/slot 连接保持不变。

不能复制第二套 QAction，也不能通过按钮重新实现加载布局、模块显示或样式切换。
迁移的是菜单的父控件和位置，不是业务功能。

### 8.3 窗口按钮行为

- 最小化：调用 `showMinimized()`。
- 最大化/还原：普通状态调用 `showMaximized()`，最大化状态调用 `showNormal()`。
- 关闭：调用 `close()`。
- 监听 `QEvent::WindowStateChange`，同步最大化/还原图标、无障碍名称和 tooltip。
- 按钮图标优先使用 `QStyle::SP_TitleBarMinButton`、
  `SP_TitleBarMaxButton`、`SP_TitleBarNormalButton` 和 `SP_TitleBarCloseButton`。

## 9. 第四批：实现 Windows 无边框行为

### 9.1 启用无边框窗口

在 `MainWindow` 初始化阶段设置 `Qt::FramelessWindowHint`，同时保留窗口系统菜单、
最小化、最大化和关闭能力需要的窗口 flags。不能把主窗口改成工具窗口，也不能改变
任务栏图标归属。

### 9.2 WM_NCHITTEST 命中顺序

在 Qt 5.15.2 的 `MainWindow::nativeEvent(const QByteArray&, void*, long*)` 中处理
`WM_NCHITTEST`，顺序必须固定：

1. 非最大化状态先判断四个角；
2. 再判断上、下、左、右四条边；
3. 如果坐标位于菜单、最小化、最大化、关闭按钮或其他可交互子控件，返回
   `HTCLIENT`；
4. 如果坐标位于标题栏空白区域，返回 `HTCAPTION`；
5. 其他客户区返回 `HTCLIENT`。

角和边应使用 Qt 逻辑像素定义稳定命中宽度，并在 100%、125%、150% 缩放下验证。
最大化状态下不能返回边缘缩放命中值。

将标题栏空白区域交给 `HTCAPTION` 后，窗口移动、双击最大化和拖到屏幕边缘贴靠应
由 Windows 完成，不需要自己循环修改窗口坐标。

### 9.3 WM_GETMINMAXINFO 最大化工作区

处理 `WM_GETMINMAXINFO`：

- 通过当前窗口所在显示器取得 monitor 信息；
- 使用工作区 `rcWork` 而不是完整显示器 `rcMonitor`；
- 设置最大化位置和尺寸；
- 确保任务栏在顶部、底部、左侧或右侧时都不会被覆盖；
- 切换到另一台显示器后重新按目标显示器计算。

所有 Win32 include、常量和分支使用 `#ifdef Q_OS_WIN` 限定，不能让 Windows 细节
扩散到模块或 SDK 接口。

## 10. 第五批：标题栏和分隔条 QSS

在 `TechDashboard.qss` 中为新标题栏增加独立、低耦合规则：

- 标题栏：深色实底和底部分隔线；
- 品牌名称：比普通菜单略亮，但不使用大字号；
- 菜单：沿用第 6 章的可读状态；
- 最小化/最大化：悬浮时使用中性深色背景；
- 关闭按钮：普通状态保持克制，悬浮时使用红色背景和白色图标；
- 所有按钮使用稳定宽高，不因 hover、pressed 或图标切换改变布局。

不使用 `QToolButton` 全局规则直接覆盖窗口控制按钮，应通过对象名或明确动态属性限定，
避免影响工具栏、模块按钮和 Dock 按钮。

## 11. 自动测试计划

在现有 `QFrameworkTests` 中增加或扩展以下测试。

### 11.1 标题栏结构和菜单复用

- `MainWindow` 能找到唯一的自绘标题栏和唯一的菜单栏。
- 菜单仍为“文件、模块、样式”三组。
- 原有加载、保存、模块管理、模块动态 QAction、选择 QSS 和重新加载 QAction 仍存在。
- 不再额外创建原来的独立顶层菜单行。
- 菜单 QAction 的 `moduleId` 和勾选语义保持不变。

### 11.2 窗口按钮

- 三个按钮对象名稳定且可找到。
- 点击最小化后窗口进入 `Qt::WindowMinimized`。
- 点击最大化后窗口进入最大化状态，按钮变为“还原”。
- 点击还原后回到普通状态，按钮恢复为“最大化”。
- 点击关闭触发正常 `QCloseEvent`，不直接调用进程强制退出。

### 11.3 TechDashboard 样式

- QSS 是有效 UTF-8、括号配对正确且能被 `StyleManager` 成功加载。
- 加载时没有未知属性、无效伪状态或无效 subcontrol 警告。
- 应用 QSS 后 `QStyle::PM_DockWidgetSeparatorExtent` 不小于计划命中尺寸。
- 测试结束恢复原 `qApp->styleSheet()`，不能污染后续测试。

### 11.4 现有回归

- `layoutPersistenceAndDockingRules` 继续通过。
- `styleSheetReloadAndFailureRecovery` 继续通过。
- 菜单显示意图、标签切换、late-ready、ProcessUi 尺寸和重启相关测试继续通过。
- 完整 `QFrameworkTests` 必须通过，不能只运行新增测试。

自动测试不能代替真实窗口拖动。Qt offscreen 平台过去不能可靠证明 Dock 和窗口鼠标行为，
不得把 offscreen 进程退出码当作本批人工 UI 通过。

## 12. 人工 UI 验收

必须在真实 Windows 桌面逐项执行。

### 12.1 标题栏

1. 启动主窗口，确认原生白色标题栏完全消失。
2. 确认最上方只有一行：左侧菜单，右侧三个窗口按钮，不显示额外图标和 `QFramework` 文字。
3. 在标题栏空白处拖动窗口。
4. 双击空白处最大化，再双击还原。
5. 分别拖动四条边和四个角缩放。
6. 拖到屏幕左、右和顶部，检查贴边布局。
7. 最大化后确认不覆盖任务栏。
8. 点击最小化、最大化、还原、关闭。
9. 检查 `Alt+F4`；检查并记录 `Alt+Space` 结果。

### 12.2 菜单

1. 依次悬浮“文件、模块、样式”，确认文字清晰。
2. 依次打开三个菜单，确认展开状态和下拉菜单均清晰。
3. 使用 Alt 助记键操作菜单。
4. 通过模块菜单显示、隐藏和切换两个 Dock，确认原有功能未变。
5. 选择并重新加载 QSS，确认标题栏和菜单不会重复创建或改变高度。

### 12.3 Dock 分隔条

1. 两个 Dock 左右停靠，拖动竖向分隔条。
2. 两个 Dock 上下停靠，拖动横向分隔条。
3. 检查普通、悬浮和拖动颜色。
4. 分别验证 InProcessUi/InProcessUi 和 InProcessUi/ProcessUi 组合。
5. 拖到最小尺寸，确认不能继续压缩但可以向反方向恢复。
6. 保存布局、改变位置、重新加载，确认布局关系和尺寸恢复。

### 12.4 DPI 和多显示器

- 至少检查 100%、125%、150% 缩放。
- 有条件时在两台不同缩放比例的显示器间拖动。
- 检查标题、菜单和窗口按钮没有重叠、截断或跳位。
- 检查最大化工作区按当前显示器任务栏正确计算。

## 13. 构建和验证命令

### 13.1 最快相关构建

```powershell
$env:TEMP='D:\QFrameworkv2\.tmp\msbuild'
$env:TMP='D:\QFrameworkv2\.tmp\msbuild'
& 'D:\Program Files (x86)\Microsoft Visual Studio\2017\Community\MSBuild\15.0\Bin\MSBuild.exe' `
  'D:\QFrameworkv2\QFramework.sln' /t:QFrameworkTests /m:1 `
  /p:Configuration=Debug /p:Platform=x64 `
  /p:QtInstallDir='D:\Qt\5.15.2\msvc2019_64' `
  /p:TrackFileAccess=false /v:minimal
```

先运行新增测试和直接相关现有测试，再运行完整 Debug 测试：

```powershell
$env:QT_QPA_PLATFORM='windows'
& 'D:\QFrameworkv2\Bin\v141\x64\Debug\QFrameworkTests.exe' `
  layoutPersistenceAndDockingRules `
  styleSheetReloadAndFailureRecovery

& 'D:\QFrameworkv2\Bin\v141\x64\Debug\QFrameworkTests.exe'
```

新增测试的最终函数名以实际实现为准，实施者不得在测试未加入前把计划名称当作已存在
命令。

### 13.2 Release x64

Debug 完整测试通过后执行：

```powershell
$env:TEMP='D:\QFrameworkv2\.tmp\msbuild'
$env:TMP='D:\QFrameworkv2\.tmp\msbuild'
& 'D:\Program Files (x86)\Microsoft Visual Studio\2017\Community\MSBuild\15.0\Bin\MSBuild.exe' `
  'D:\QFrameworkv2\QFramework.sln' /m:1 `
  /p:Configuration=Release /p:Platform=x64 `
  /p:QtInstallDir='D:\Qt\5.15.2\msvc2019_64' `
  /p:TrackFileAccess=false /v:minimal

$env:QT_QPA_PLATFORM='windows'
& 'D:\QFrameworkv2\Bin\v141\x64\Release\QFrameworkTests.exe'
```

### 13.3 静态检查

```powershell
git -C D:\QFrameworkv2 diff --check
git -C D:\QFrameworkv2 status --short
```

同时检查：

- `TechDashboard.qss` 为 UTF-8，括号数量匹配；
- 没有无效 QSS selector、pseudo-state 或 subcontrol 警告；
- 没有新增第三方依赖和网络调用；
- 没有破坏当前工作树中的无关修改；
- Windows 专用代码都位于 `#ifdef Q_OS_WIN` 内。

## 14. 分批提交和回滚点

建议拆成以下可独立审查的批次：

1. `TechDashboard.qss` 菜单状态与 `QMainWindow::separator`。
2. `WindowTitleBar` 控件和菜单迁移。
3. `MainWindow` 无边框 Win32 命中与最大化工作区。
4. 自动测试、人工验收记录和相关文档收口。

每批开始前记录 `git status` 和目标文件 diff；每批结束后运行直接相关测试。

回滚原则：

- QSS 修复可以独立回滚，不影响窗口业务逻辑。
- 标题栏类和 `Qt::FramelessWindowHint` 必须作为同一完整批次回滚，不能只删按钮而
  保留无边框 flag。
- Windows 原生事件处理和无边框 flag 必须保持配套，否则会得到无法移动或无法缩放
  的窗口。
- 本批不改变布局格式，因此不需要升级或回退 `.qflayout` 版本。
- 回滚后必须重新运行完整 Debug 测试并人工检查窗口行为。

## 15. 完成标准

只有同时满足以下条件，本计划才能标记为完成：

- 菜单普通、悬浮、展开、禁用状态均清晰可读。
- 左右和上下 Dock 分隔条均容易命中并可拖动。
- 原生白色标题栏已替换为单行科技风自绘标题栏。
- 左侧现有菜单功能完整，右侧三个窗口按钮工作正常。
- 窗口可以移动、八方向缩放、双击最大化和拖动贴边。
- 最大化不覆盖任务栏，多显示器/DPI 没有明显错位。
- 原有布局、模块显示意图、ProcessUi 嵌入和重启测试没有回归。
- Debug x64 构建、直接测试和完整测试通过。
- Release x64 构建和完整测试：待 QtMsBuild `CriticalSection` 访问错误修复后重新验证。
- `git diff --check` 通过。
- 真实 Windows 桌面人工验收完成并记录；未执行项必须明确标为未验证，不能写成通过。

## 16. 新会话实施提示词

下面内容可以直接复制到新的代码实施会话：

```text
请在 D:\QFrameworkv2 中直接实施“菜单可读性、Dock 分隔条和无边框标题栏”修改。
这是代码实施任务，不是重新只写计划。

开始前：
1. 完整阅读 D:\QFrameworkv2\修改计划.md。
2. 检查 git status 和所有目标文件现有 diff；保留用户修改和未跟踪文件，不得回滚或覆盖。
3. 重新核对 MainWindow、ManagedDockWidget、ProcessWindowHost、TechDashboard.qss 和现有测试，发现代码漂移时按当前代码做最小调整。

必须完成：
1. 在 TechDashboard.qss 中补全 QMenuBar 正常、悬浮、按下、禁用状态，解决顶部菜单悬浮文字难以辨认。
2. 增加 QMainWindow::separator 普通/悬浮规则和约 6px 命中尺寸，保留现有 QSplitter::handle。
3. 新增 WindowTitleBar，将现有文件、模块、样式菜单迁移到顶部左侧，右侧增加最小化、最大化/还原和关闭按钮；复用现有 QAction，不复制业务逻辑。
4. MainWindow 使用 Qt::FramelessWindowHint，并在 Windows 下正确处理 WM_NCHITTEST 和 WM_GETMINMAXINFO，保留拖动、八方向缩放、双击最大化、任务栏工作区和拖动贴边。
5. 不修改 Dock 显示意图、布局格式、模块/IPC/日志/进程逻辑、ProcessUi 嵌入协议和 180x120 最小尺寸。
6. 增加直接 QtTest，先运行最快相关测试，再运行完整 Debug x64、Release x64 和 git diff --check。
7. 在真实 Windows 桌面按计划逐项验证菜单、窗口按钮、拖动缩放、贴边、左右/上下 Dock 分隔条、多显示器和 100%/125%/150% DPI；offscreen 不能替代人工 UI 验收。

代码可读性和注释要求：
1. 我已经很久没有使用 C++，新增实现必须尽量使用 Qt 5.15.2 的类、QObject 父子关系、事件和 signal/slot，不要引入 std::thread、std::mutex、协程、复杂模板、多层嵌套 lambda 或新的泛型框架。
2. Win32 代码只能放在 MainWindow 的小范围 Q_OS_WIN 分支中；尽早把坐标转换为 QPoint/QRect，并把命中判断拆成有清晰名称的小函数。
3. 为类职责、控件所有权、菜单 QAction 复用、窗口按钮信号流、窗口状态变化、WM_NCHITTEST 判断顺序、HTCLIENT/HTCAPTION、八方向缩放、DPI 坐标、任务栏工作区、QMainWindow::separator 与 QSplitter::handle 区别以及测试边界添加详细完整的中文注释。
4. 注释重点解释原因、Qt 数据流和平台取舍，不要只把 setText、setIcon、赋值和空指针判断翻译成无信息注释。
5. 使用明确的函数和变量名称，避免 tmp、flag1 等含糊名称；注释必须与最终行为一致。

保持小范围、可审查的 diff，不引入第三方依赖、遥测或网络调用。完成后用容易理解的中文报告修改文件、关键 Qt 数据流、构建测试结果和仍未执行的人工门槛。
```

## 17. 2026-08-05 实施记录

本批已经按上述计划写入源码，现有 QAction、Dock 对象名、布局状态和 ProcessUi 协议
没有改名或改格式：

- `config/Styles/TechDashboard.qss` 增加 `QMenuBar` 正常/悬浮/选中/按下/禁用状态、
  `QMainWindow::separator` 的 6px 普通/悬浮规则，以及无边框标题栏和三枚按钮样式。
- Release 运行目录会从 `Bin\\v141\\x64\\Release\\config\\Styles\\TechDashboard.qss`
  读取样式；本次发现该副本落后于源码，已用当前源码 QSS 同步，避免 Release 继续加载旧规则。
- 新增 `Source/QFramework/Ui/WindowTitleBar.h/.cpp`。它由 Qt 父子对象关系管理控件，
  只保留 `QMenuBar` 和右侧三个窗口按钮；复用 MainWindow 原有 QAction，只发出最小化、
  最大化/还原、关闭信号，不创建图标或 `QFramework` 文字标签。
- `MainWindow` 使用 `Qt::FramelessWindowHint`，在 Windows 小范围 `Q_OS_WIN` 代码中
  处理 `WM_NCHITTEST` 八方向缩放/标题栏拖动和 `WM_GETMINMAXINFO` 工作区最大化。
- `QFramework.vcxproj` 登记标题栏的 Qt MOC/C++ 文件；`tst_Baseline` 增加标题栏按钮、
  菜单复用、无图标/名称回归和 TechDashboard 分隔条样式测试。

已完成的自动证据：Debug `QFrameworkTests` 目标构建成功；新增测试加布局/QSS 回归为
`6 passed, 0 failed`；完整 Debug x64 `QFrameworkTests` 为 `50 passed, 0 failed, 0 skipped,
0 blacklisted`。本次 Release 重建在 QtMsBuild `CriticalSection` 任务阶段失败，错误为
`EventWaitHandle.OpenExisting` 访问被拒绝，未进入本批源码编译，因此不能把现有旧 Release
可执行文件当作本批验证结果。`git diff --check` 已通过。真实 Windows 桌面上的窗口拖动、
四边八角缩放、双击最大化、任务栏/贴边、
左右/上下 Dock 鼠标拖动及 100%/125%/150% DPI、多显示器检查尚未由自动测试替代，
必须单独记录为人工验收结果。
