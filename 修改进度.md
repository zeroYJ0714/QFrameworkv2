# QFramework 修改进度

## 2026-08-05：无边框主窗口、菜单可读性和 Dock 分隔条

状态：代码已实施；聚焦 Debug QtTest 和完整 Debug 已通过。Release 重建目前被 QtMsBuild
临界区环境错误阻塞，真实桌面人工 UI 验收仍未完成。未把 Windows 平台自动测试当成鼠标
人工验收的替代品。

### 本批修改

1. `config/Styles/TechDashboard.qss` 为 `QMenuBar` 补充正常、悬浮、按下、禁用四种
   状态的文字色和背景色，解决顶部菜单悬浮后浅色文字落在浅色系统背景上的问题。
2. 增加 `QMainWindow::separator` 普通/悬浮样式，宽高约 6px；保留
   `QSplitter::handle` 给真实 `QSplitter` 使用。Dock 原有 `180 x 120` 最小尺寸和
   `DockWidgetMovable` 策略未改变。
3. 新增 `WindowTitleBar` Qt Widget：左侧只保留现有菜单栏，右侧为 Qt `QToolButton`
   标准最小化/最大化/关闭图标，不创建额外图标和 `QFramework` 文字。它只发 signal，MainWindow 仍负责
   QAction、模块、进程和关闭清理。
4. `MainWindow` 设置 `Qt::FramelessWindowHint`，通过 `changeEvent` 同步最大化/还原
   图标；Windows 下用 `WM_NCHITTEST` 交给系统处理拖动、八方向缩放和贴边，用
   `WM_GETMINMAXINFO` 按显示器工作区计算最大化尺寸，避免覆盖任务栏。
5. `Tests/QFrameworkTests/tst_Baseline.{h,cpp}` 增加标题栏结构/按钮状态测试，以及真实
   TechDashboard QSS 的菜单状态、分隔条选择器和命中尺寸测试。

### 已改文件

- `Source/QFramework/Ui/MainWindow.{h,cpp}`
- `Source/QFramework/Ui/WindowTitleBar.{h,cpp}`
- `Source/QFramework/QFramework.vcxproj`
- `Tests/QFrameworkTests/tst_Baseline.{h,cpp}`
- `config/Styles/TechDashboard.qss`
- `修改计划.md`、`修改进度.md`
- 其他 Markdown 文档已补充本批当前 UI 行为说明，见各文档末尾的“2026-08-05”小节。

### 当前验证

已执行：

- VS2017 v141 + Qt 5.15.2 Debug x64 `QFrameworkTests` 目标构建成功。
- `framelessTitleBarAndWindowControls`、`techDashboardMenuAndDockStyle`、
  `layoutPersistenceAndDockingRules`、`styleSheetReloadAndFailureRecovery`：
  `6 passed, 0 failed, 0 skipped, 0 blacklisted`（含 init/cleanup）。
- 测试使用 `QT_QPA_PLATFORM=windows`；offscreen 不用于宣称真实 Dock/窗口鼠标通过。

- Debug x64 完整解决方案构建成功，完整 `QFrameworkTests`：`50 passed, 0 failed,
  0 skipped, 0 blacklisted`。
- Release x64 已尝试使用相同 Qt 5.15.2/VS2017 配置重建，但在
  `C:\Users\Administrator\AppData\Local\QtMsBuild\qt_globals.targets(713,5)` 的
  `CriticalSection` 任务失败：`EventWaitHandle.OpenExisting` 报“访问被拒绝”，未进入
  本批源码编译；现有 Release 可执行文件未作为本批结果使用。
- 已核对并同步 Release 实际运行目录的 `config/Styles/TechDashboard.qss`；该副本之前没有
  `QMenuBar::item:hover`，是 Release 悬停文字仍看不清的直接原因。Release `QFramework.ini`
  仍保留用户设置的 `File=Styles/TechDashboard.qss`。

`git diff --check` 已通过；仍待真实桌面菜单悬浮、窗口移动/缩放/贴边、左右/上下
Dock 分隔条拖动和 DPI/多显示器验收。
