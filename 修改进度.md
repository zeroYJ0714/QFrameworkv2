# QFramework 修改进度

## 2026-08-06：INI 动态预设布局

### 已完成

- `FrameworkConfig` 支持连续 `[Layout.1]`、`[Layout.2]` 等分组，读取 `Name/File` 并按
  INI 所在 `config` 目录解析相对路径。
- 删除旧 `[Layout]/StartupFile` 的运行语义；示例配置改为 `[Layout.1]`。
- `LayoutManager` 增加无副作用布局文件校验，并把加载/保存是否更新活动路径变为显式的
  `LayoutActivation` 策略。
- `MainWindow` 在左上角“布局”菜单直接创建预设 QAction，支持重复名称、空名称回退为
  `布局n`、互斥勾选、置灰项悬浮提示和警告日志。
- 启动自动尝试 `Layout.1`；启动失败不弹框并保持默认窗口。预设切换失败会回滚原选择，
  失败项仍可再次点击。
- 手动加载和布局另存为不改变当前预设；“保存当前布局”只覆盖当前选中的预设文件，
  没有活动预设时按钮置灰。
- 新增 QtTest，覆盖配置解析、菜单构建、启动加载、切换失败回滚、手动加载/另存为活动
  预设保持和保存当前布局。
- 同步配置参考、操作指南、架构说明、模块接口参考、四类模块教程、文档索引和实施计划。

### 自动验证

- Debug x64 `QFrameworkTests` 目标构建成功。
- 布局聚焦回归（`configIsReadOnlyAndResolvesPaths`、`layoutPresetsParseAndBuildMenu`、
  `layoutPresetFailureKeepsSelection`、`manualLayoutOperationsKeepPresetActive`）：
  `6 passed, 0 failed, 0 skipped, 0 blacklisted`。
- 完整 Debug x64 QtTest：`58 passed, 0 failed, 0 skipped, 0 blacklisted`。
- Release x64 `QFrameworkTests` 目标构建成功，完整 QtTest：
  `58 passed, 0 failed, 0 skipped, 0 blacklisted`。
- `git diff --check` 通过；仅有 Git 关于工作区 LF/CRLF 转换的提示，没有空白错误。

### 待继续验证

- 真实 Windows 桌面上的菜单悬浮、预设勾选、置灰提示、重复点击、运行期间文件损坏后的
  回滚及多显示器/DPI 人工检查；自动测试不能替代这些检查。
