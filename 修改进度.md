# QFramework 修改进度

## 2026-08-04：跨进程有界帧队列与日志批量刷新

状态：已按 `修改计划.md` 完成代码、配置、文档和 Qt Test 实施；Debug/Release x64 构建及完整测试通过。

### 已完成批次

1. 测试入口与辅助：增加真实 `ProcessRuntime` 测试模块和慢消费者场景，覆盖父到子/子到父 Latest、Reliable、inline/shared、双向 IPC、日志定时刷新、显式 flush 和停止尾日志。
2. 父到子队列：`ProcessSupervisor` 的每个 Entry 使用按主题有界 `QQueue`；Latest 只删除同主题等待帧，Reliable 不覆盖；合并 Qt 唤醒事件、限制在途窗口，并在消息进入子进程有界队列后的 delivery ACK 释放槽位和共享内存。
3. 子进程队列：注册确认下发主题容量/大小/策略；`ProcessRuntime` 输入队列和发布队列均有界，发布路径不再为每帧携带完整 `QByteArray` 的 queued event；publish ACK 在父侧接收/拒绝后释放槽位和共享内存。停止、断线、故障和重启路径会清理并唤醒队列。
4. 日志：新增 `LoggingConfig::flushIntervalMs` 和 `Logger::start()` 重载；默认/非法值回退 100 ms。普通记录按间隔批量写入并整批刷新；显式 flush、Fatal、滚动和停止立即刷新。
5. 文档与配置：同步 INI 参考、架构说明、SDK `publish()` 语义和本进度记录；`config/QFramework.ini` 增加 `FlushIntervalMs=100`。

### 修改文件

- `Source/QFramework/Process/ProcessSupervisor.{h,cpp}`
- `Source/QFramework/Process/ProcessRuntime.cpp`
- `Source/QFrameworkSdk/ProcessRuntime.h`
- `Source/QFramework/Logging/Logger.{h,cpp}`
- `Source/QFramework/Config/FrameworkConfig.{h,cpp}`
- `Source/QFramework/Core/FrameworkRuntime.cpp`
- `Tests/QFrameworkTests/tst_Baseline.{h,cpp}`
- `config/QFramework.ini`
- `Docs/Reference/框架配置文件参考.md`
- `Docs/Reference/模块开发接口参考.md`
- `Docs/Explanation/架构说明.md`
- `修改进度.md`

未修改既有 `MessageBus` 无限等待问题、日志保留策略、插件完整系统、中央工作区、Dock、侧边栏和构建工程文件。

### 验证结果

语法/MOC 预检：

- VS2017 v141 `cl.exe /Zs /std:c++17`：ProcessSupervisor、ProcessRuntime、Logger、FrameworkConfig、FrameworkRuntime 和 `tst_Baseline.cpp` 通过。
- Qt 5.15.2 `moc`：`ProcessRuntime.h`、`ProcessSupervisor.h`、`tst_Baseline.h` 通过。

Debug x64：

```powershell
$env:TEMP='D:\QFrameworkv2\.tmp\msbuild'
$env:TMP='D:\QFrameworkv2\.tmp\msbuild'
& 'D:\Program Files (x86)\Microsoft Visual Studio\2017\Community\MSBuild\15.0\Bin\MSBuild.exe' `
  'D:\QFrameworkv2\QFramework.sln' /m /p:Configuration=Debug /p:Platform=x64 `
  /p:QtInstallDir='D:\Qt\5.15.2\msvc2019_64' /v:minimal
& 'D:\QFrameworkv2\Bin\v141\x64\Debug\QFrameworkTests.exe'
```

结果：构建成功；完整 Qt Test `23 passed, 0 failed`。

Release x64 使用同一命令、将 `Configuration=Release` 和测试路径切换到 `Bin\v141\x64\Release`；结果：构建成功；完整 Qt Test `23 passed, 0 failed`。

调度稳定性补充验证：Debug `processReliableQueueDoesNotOverwrite` 连续运行 5 次，均为 `3 passed, 0 failed`（含 init/cleanup）；测试按 Reliable 的真实语义检查第一条不被覆盖、实际交付结果为发布序列的严格递增子序列，允许队列满拒绝形成缺口。

补丁检查：`git diff --check` 通过。

直接按计划中的原始 MSBuild 命令（未指定本机 Qt 注册目录/临时目录）会在环境层报 `MSB4018 Microsoft.Build.Utilities.FileTracker`；使用现有 Qt 目录并为构建进程指定可写 `TEMP/TMP` 后成功，未修改构建体系。

未执行真实摄像头/硬件链路、长时间压力 soak、人工 Dock 拖拽和完整发布包人工验收；本次 Qt Test 使用本地合成模块和 IPC，不替代这些验证。

## 2026-08-04：初学者注释与中文文档文件名

状态：已完成本轮文档整理和注释补充；没有改变 IPC、队列、ACK 或日志刷新行为。Debug/Release x64 构建和完整 Qt Test 均通过。

### 本轮变更

1. 在 `ProcessSupervisor` 和 `ProcessRuntime` 的数据结构、入队、批量排空、在途计数、delivery ACK、publish ACK、共享内存回收以及停止/故障清理边界增加中文解释，说明 ACK 的确认时点和槽位释放关系。
2. 在 `Logger` 的 Worker 队列、刷新序列号、100 ms 定时批处理、显式刷新、Fatal、滚动和停止等待路径增加中文解释；在配置读取和框架启停接线处说明数据流。
3. 在相关 Qt Test 增加测试目的、慢消费者、小容量和 inline/shared 阈值的说明，便于初学者把测试现象对应到实现。
4. 将仓库内英文 Markdown 文件名改为中文，并同步文档索引、修改计划和本进度文件中的路径：`Docs/文档索引.md`、`Docs/Explanation/架构说明.md`、`Docs/Guides/操作与调试指南.md`、`Docs/Reference/模块开发接口参考.md`、`Docs/Reference/框架配置文件参考.md`、`Docs/Tutorials/创建四类模块教程.md`。

### 说明

本轮注释限定在本次新增的跨进程有界队列、ACK、共享内存和日志刷新代码及其测试/配置接线，没有机械注释整个历史代码库；根目录已有中文 Markdown 文件名保持不变。

### 本轮验证

- 旧英文 Markdown 文件名全仓搜索无结果；仓库中的 9 个 `.md` 文件名均为中文，文档索引的 5 个相对链接目标均存在。
- Debug x64：VS2017 v141 + Qt 5.15.2 构建成功；完整 `QFrameworkTests` 为 `23 passed, 0 failed`。
- Release x64：VS2017 v141 + Qt 5.15.2 构建成功；完整 `QFrameworkTests` 为 `23 passed, 0 failed`。
- `git diff --check` 通过。本机部署目录只有 Qt `windows` 平台插件，因此测试使用 `QT_QPA_PLATFORM=windows`；`offscreen` 插件未部署。

## 2026-08-04：全仓手写代码详细中文注释

状态：已按用户补充要求，把注释范围从“本次新增 ACK/队列代码”扩大到整个仓库的可维护手写代码；注释修改不改变程序行为。

### 覆盖范围

1. 已覆盖 58 个手写 C/C++ 文件，当前约 9485 行（包含新增注释）：
   - `Source/QFramework`：配置、核心生命周期、日志、消息总线、插件、进程监督和全部 UI；
   - `Source/QFrameworkSdk`：四类模块基类、模块端点、子进程运行时和导出接口；
   - `Source/QFrameworkProtocols`：手写主题、版本接口；
   - `Source/QFrameworkApp`：主应用入口；
   - `Modules`：四类示例模块及两个子进程入口；
   - `Tests/QFrameworkTests`：全部测试替身、辅助函数、测试过程和双身份入口。
2. 额外补充 3 个非生成 `.proto` 协议源文件的消息、枚举和字段说明。
3. 明确不修改 `ThirdParty` 和 `Source/QFrameworkProtocols/Generated`。前者属于外部依赖，后者由 `protoc` 自动生成；本轮构建重新执行协议生成后，生成目录内容没有进入 Git diff。

### 注释层级

- 每个文件说明自身职责以及它在框架数据流中的位置。
- 类、结构体和接口说明对象所有权、线程归属、调用时机、参数/返回值和失败语义。
- 关键函数和分支说明“为什么这样处理”，重点包括 Latest/Reliable、双向有界队列、delivery ACK、publish ACK、inline/shared-memory、日志批量刷新、进程注册/心跳/重启、插件生命周期和 UI 线程切换。
- 每个 Qt Test 说明“准备输入—执行动作—预期断言”，故障子进程的阶段性错误码也有对应解释。
- 未对简单语法机械重复代码含义，避免注释噪声；所有可维护文件均已包含面向初学者的中文职责或接口说明。

### 中文 Markdown 文件名

仓库现有 9 个 `.md` 文件名均为中文；英文旧路径已改名并同步文档链接。Git 未暂存时重命名显示为旧文件 `D`、中文新文件 `??`，属于同一批未提交工作，不是内容丢失。

### 本轮验证

- Debug x64：VS2017 v141 + Qt 5.15.2 构建成功；完整 `QFrameworkTests` 为 `23 passed, 0 failed`。
- Release x64：构建成功。第一次全量运行中 `processDebuggerWaitDefersHeartbeat` 出现一次注册调度超时（其余 22 项通过）；该项单独复跑为 `3 passed, 0 failed`，随后完整套件复跑为 `23 passed, 0 failed`。没有为该一次性抖动修改业务超时或放宽断言。
- 构建时 `windeployqt` 提示 `VCINSTALLDIR is not set`，但命令退出码为 0，目标程序、插件和测试程序均成功生成。
- `git diff --check` 通过。

未执行真实摄像头/硬件链路、长时间压力 soak、人工 Dock 拖拽和完整发布包人工验收；注释本身不替代这些硬件或人工验证。

## 2026-08-05：第 16 章——用户显示意图与布局恢复

状态：本批代码、7 项直接 Debug x64 Qt Test 和直接相关文档已完成；完整
Debug/Release 套件及真实桌面人工操作留在第 20/24 章统一收口。

### 实际修改

1. `MainWindow` 把 `requestedDockVisibility_`（用户希望显示）与
   `uiAvailable_`（模块界面已 ready）分开。模块 QAction 勾选时显示或保留
   pending，取消勾选时立即隐藏；程序同步 QAction 使用 `QSignalBlocker`。
2. `ManagedDockWidget` 只通过 `closeRequested()` 表示关闭按钮请求；不再用
   `visibilityChanged(false)` 推断用户意图，因此标签切换不会取消菜单勾选。
3. `LayoutManager` 保存 `requestedVisible`，同时保留兼容字段 `visible`；新文件
   标记 `visibilitySemantics=userIntent`。加载成功后才整体替换显示意图，失败时
   保留原 geometry、Dock state、显示意图和活动文件路径。
4. 子进程/插件 UI 暂不可用时 Dock 隐藏但 pending 不丢失；ready 后自动补显示，
   明确隐藏时不补显示。非用户触发的补显示会抬回原活动标签，避免抢焦点。

修改文件：

- `Source/QFramework/Ui/MainWindow.{h,cpp}`
- `Source/QFramework/Ui/ManagedDockWidget.{h,cpp}`
- `Source/QFramework/Ui/LayoutManager.{h,cpp}`
- `Tests/QFrameworkTests/tst_Baseline.{h,cpp}`
- `修改计划.md`、`修改进度.md`、`开发计划.md`
- `Docs/Explanation/架构说明.md`
- `Docs/Guides/操作与调试指南.md`
- `Docs/Reference/框架配置文件参考.md`

### 命令与结果

首次并行方案构建在本机 VS2017 `FileTracker` 层报 `MSB4018`；把构建进程的
`TEMP/TMP` 指向 `D:\QFrameworkv2\.tmp\msbuild` 后，本批 `QFramework.dll`
已编译链接。随后使用串行测试目标完成真实构建：

```powershell
$env:TEMP='D:\QFrameworkv2\.tmp\msbuild'
$env:TMP='D:\QFrameworkv2\.tmp\msbuild'
& 'D:\Program Files (x86)\Microsoft Visual Studio\2017\Community\MSBuild\15.0\Bin\MSBuild.exe' `
  'D:\QFrameworkv2\QFramework.sln' /t:QFrameworkTests /m:1 `
  /p:Configuration=Debug /p:Platform=x64 `
  /p:QtInstallDir='D:\Qt\5.15.2\msvc2019_64' /p:TrackFileAccess=false /v:minimal
```

构建成功；`windeployqt` 仍提示 `VCINSTALLDIR is not set`，不影响目标生成。

直接测试：`moduleMenuTogglesRequestedVisibility`、
`dockCloseClearsRequestedVisibility`、`tabSwitchKeepsRequestedVisibility`、
`layoutPreservesTabbedVisibilityIntent`、`layoutDefersUnavailableDockUntilReady`、
`layoutHiddenDockStaysHiddenAfterReady`、
`legacyLayoutLoadsWithoutCorruptingCurrentState`。结果：`9 passed, 0 failed`
（包含 init/cleanup）。测试使用 Windows 平台插件，未使用 offscreen。
既有 `layoutPersistenceAndDockingRules` 回归也单独通过：`3 passed, 0 failed`。

### 本批文档审计与剩余门槛

- 已同步：修改计划、修改进度、开发计划、架构说明、操作与调试指南、框架配置参考。
- 已核查，本批无需修改：模块开发接口参考、创建四类模块教程、文档索引。
- 尚未声明通过：完整 Debug/Release、真实桌面菜单/标签/布局/ProcessUi 晚到人工操作。

## 2026-08-05：第 17 章——异步停止和重启状态机

状态：本批代码、7 项新增直接 Debug x64 Qt Test、8 项既有进程/IPC 回归和直接
相关文档已完成；完整 Debug/Release 与真实桌面人工操作仍留在第 20/24 章收口。

### 实际修改

1. `ProcessSupervisor` 以 `LifecyclePhase` 和 `StopPurpose` 代替冲突布尔值；每个
   Entry 保存独立 startup/stop `QDeadlineTimer`、generation、pending start、
   stopAck 和 operation busy。
2. `startAll()`、`requestStop()`、`requestRestart()` 均立即返回；注册、started、
   stopAck、进程 finished/error 和监督定时器异步推进。启动批次以
   `startupBatchFinished` 结算后，`FrameworkRuntime` 才放开 MessageBus 消费。
3. 停止入口先关闭父到子队列及 MessageBus 单模块入口，再清等待项、建立独立
   deadline，最后发 stop。普通运行路径不再使用嵌套事件循环或同步进程等待。
4. 最终退出先并行发 stop，再用一个总 deadline 轮询全部 `QProcess`；超时升级
   `terminate()`/`kill()` 作用于完整子进程，不处理普通 GUI 重启事件。
5. 模块管理对话框保存重启按钮；生命周期 busy 时禁用，`restartFinished` 后恢复。

修改文件：

- `Source/QFramework/Process/ProcessSupervisor.{h,cpp}`
- `Source/QFramework/Core/FrameworkRuntime.{h,cpp}`
- `Source/QFramework/Ui/MainWindow.{h,cpp}`
- `Source/QFramework/Ui/ModuleManagerDialog.{h,cpp}`
- `Tests/QFrameworkTests/tst_Baseline.{h,cpp}`
- `修改计划.md`、`修改进度.md`、`开发计划.md`
- `Docs/Explanation/架构说明.md`
- `Docs/Guides/操作与调试指南.md`
- `Docs/Reference/框架配置文件参考.md`

### 命令与结果

使用与第 16 批相同的 Debug x64 单进程 MSBuild 命令，`QFrameworkTests` 构建成功；
仍只有既有 `windeployqt: VCINSTALLDIR is not set` 非致命警告。

新增直接测试：`processRestartRequestDoesNotBlockGui`、
`processRejectsDuplicateRestartRequest`、`processStopClosesIngressImmediately`、
`processDeadlinesAreIndependentPerEntry`、`processStopAckAdvancesState`、
`processShutdownUsesOneTotalDeadline`、
`processStartupBatchCompletesAsynchronously`。结果：`9 passed, 0 failed`（含
init/cleanup，5226 ms）。

既有回归：`processIpcAndSupervision`、两个 Latest/Reliable 测试、双向
inline/shared 测试、invalid token、registration timeout、heartbeat timeout、
debugger wait。结果：`10 passed, 0 failed`（含 init/cleanup，10916 ms）。两组均
使用 Qt `windows` 平台插件，没有使用 offscreen。

### 本批文档审计与剩余门槛

- 已同步：修改计划、修改进度、开发计划、架构说明、操作与调试指南、框架配置参考。
- 已核查，本批无需修改：模块开发接口参考、创建四类模块教程、文档索引。
- 尚未声明通过：第 18～20 批、完整 Debug/Release、`git diff --check` 最终审查及
  真实桌面重启期间 GUI 响应人工操作。

## 2026-08-05：第 18 章——有界退出与卡死回调

状态：本批代码、5 项新增直接 Debug x64 Qt Test、13 项相关回归和直接相关文档
已完成；完整 Debug/Release、最终差异审查和真实桌面人工操作仍留在第 20/24 章。

### 实际修改

1. `MessageBus::ModuleQueue::finishStop()` 返回强类型 `Stopped/TimedOut`，删除超时
   后的无限 `wait()`；全局停止以一个 `QDeadlineTimer` 结算并返回成功/超时模块
   清单。超时时仅清理并统计未执行队列项，仍运行的线程和对象不被删除。
2. `PluginManager` 根据停止报告隔离超时 DLL，跳过与卡死 `onMessage()` 并发的
   `onStop()`、队列注销和 DLL 卸载；正常模块仍按反向顺序释放。
3. `FrameworkRuntime` 只调用一次 MessageBus 全局停止；安全组件停止后，在应用
   退出边界通过可替换测试 handler 的 `ProcessFailFast` 请求进程级 fail-fast。
4. `ProcessRuntime::MessageQueue` 删除 `QThread::terminate()`。子进程回调卡死时
   保留队列和 endpoint，发送 dirty stopAck 后让整个测试子进程退出；正常退出
   先调用 `prepareForExit()`。父进程只对完整 `QProcess` 使用 terminate/kill。
5. `ProcessSupervisor::destroyRuntime()` 仅在 MessageBus 队列已安全注销时删除
   `ProcessBridge`，并把 dirty stopAck 的 detail 写入停止结果。

修改文件：

- `Source/QFramework/Messaging/MessageBus.{h,cpp}`
- `Source/QFramework/Plugin/ModuleEndpoint.cpp`
- `Source/QFramework/Plugin/PluginManager.{h,cpp}`
- `Source/QFramework/Core/FrameworkRuntime.{h,cpp}`
- `Source/QFramework/Process/ProcessRuntime.cpp`
- `Source/QFramework/Process/ProcessSupervisor.cpp`
- `Source/QFrameworkSdk/ModuleEndpoint.h`
- `Source/QFrameworkSdk/ProcessRuntime.h`
- `Tests/QFrameworkTests/tst_Baseline.{h,cpp}`
- `修改计划.md`、`修改进度.md`
- `Docs/Reference/模块开发接口参考.md`
- `Docs/Explanation/架构说明.md`
- `Docs/Guides/操作与调试指南.md`

### 命令与结果

使用第 16、17 批相同的 Debug x64 串行 MSBuild 命令编译 `QFrameworkTests`，构建
成功；仍只有既有 `windeployqt: VCINSTALLDIR is not set` 非致命警告。

新增直接测试：`messageBusStopTimeoutIsBounded`、
`timedOutQueueIsNotDeletedOrUnloaded`、`processHungCallbackEndsWholeChild`、
`shutdownRequestsFailFastForHungInProcessModule`、
`messageQueueNeverUsesThreadTerminate`。结果：`7 passed, 0 failed`（含
init/cleanup，828 ms）。

相关回归覆盖 MessageBus 策略、插件集成、Process IPC、Latest/Reliable、双向
inline/shared，以及第 17 批 7 项状态机测试。结果：`15 passed, 0 failed`（含
init/cleanup，13608 ms）。两组均使用 Qt `windows` 平台插件。

### 本批文档审计与剩余门槛

- 已同步：修改计划、修改进度、架构说明、操作与调试指南、模块开发接口参考。
- 已核查，本批无需修改：开发计划、框架配置参考、创建四类模块教程、文档索引。
- 尚未声明通过：第 19、20 批、完整 Debug/Release、`git diff --check` 最终审查及
  真实桌面人工操作。

## 2026-08-05：第 19 章——ProcessRuntime 线程安全、控制帧和小问题

状态：本批代码、5 项新增直接 Debug x64 Qt Test、5 轮并发重复压力、6 项既有
IPC/停止回归和直接相关文档已完成；完整 Debug/Release、最终差异审查和真实桌面
人工操作仍留在第 20/24 章。

### 实际修改

1. `ProcessRuntime::queuePublish()` 不再从业务线程读取 `running_` 或 Socket 状态；
   业务线程只进入 `PublishQueue::enqueue()`。一把 Qt 指针互斥同步 enqueue 与
   stop/delete，队列自己的 `QMutex` 保护 accepting、等待项、在途表和统计。
2. `sendFrame()` 返回 `bool`，所有子进程控制帧和日志帧检查结果；Socket 读写
   集中在 Runtime 线程并用 `Q_ASSERT` 守住 QObject 线程归属。父进程 ping 写失败
   立即故障，成功后才更新 `lastPingMs`。
3. 删除无等待者的 `PublishQueue::available_` 与
   `ProcessSupervisor::outgoingChanged`。停止/故障清理分别记录 dropped、rejected、
   abandoned，并按模块限频告警、按 generation 输出一次汇总。
4. `RestartWindowMs` 比较改为 `>=`。新增公开只读 `ProcessQueueStats` 快照供诊断
   和测试使用，不改变队列容量和 Latest/Reliable 语义。
5. 并发压力首轮发现：停止过程中迟到的子到父消息可能在子进程退出后回
   publishAck，写失败被误报成手动重启失败。现仅在非停止阶段把该写失败升级为
   transport fault，既定停止阶段继续由当前 QProcess 状态机收尾。

修改文件：

- `Source/QFrameworkSdk/ProcessRuntime.h`
- `Source/QFramework/Process/ProcessRuntime.cpp`
- `Source/QFramework/Process/ProcessSupervisor.{h,cpp}`
- `Tests/QFrameworkTests/tst_Baseline.{h,cpp}`
- `修改计划.md`、`修改进度.md`
- `Docs/Explanation/架构说明.md`
- `Docs/Reference/模块开发接口参考.md`
- `Docs/Reference/框架配置文件参考.md`

### 命令与结果

Debug x64 串行 MSBuild 编译 `QFrameworkTests` 成功；仍只有既有
`windeployqt: VCINSTALLDIR is not set` 非致命警告。

新增直接测试：`processPublishGateIsThreadSafeDuringStop`、
`processControlWriteFailureTriggersFaultImmediately`、
`processStopAccountingIncludesDiscardedMessages`、
`processRestartWindowBoundaryIsInclusive`、
`processSocketAccessStaysOnRuntimeThread`。最终结果：`7 passed, 0 failed`（含
init/cleanup，1756 ms）。并发与统计组合额外重复 5 轮，5 轮均退出码 0。

既有回归覆盖 Process IPC、父到子 Latest/Reliable、双向 inline/shared、停止入口
闸门和卡死回调子进程退出。结果：`8 passed, 0 failed`（含 init/cleanup，9244 ms）。
以上测试均使用 Qt `windows` 平台插件。

### 本批文档审计与剩余门槛

- 已同步：修改计划、修改进度、架构说明、模块开发接口参考、框架配置参考。
- 已核查，本批无需修改：开发计划、操作与调试指南、创建四类模块教程、文档索引。
- 尚未声明通过：第 20 批、完整 Debug/Release、`git diff --check` 最终审查及
  真实桌面人工操作。

## 2026-08-05：第 20 章——SDK 契约、代码注释和最终文档收口

状态：代码、直接测试、完整 Debug/Release 和文档审计完成；真实桌面人工操作因
当前工具会话没有可见窗口站而未执行，保留为外部门槛。

### 实际修改

1. `Source/QFrameworkSdk/ModuleEndpoint.h` 的 `onStart()` 注释明确：回调期间允许
   `publish()`，返回 `false` 或异常时撤销发布权限。
2. `Docs/Reference/模块开发接口参考.md`、`Docs/Explanation/架构说明.md`、
   `Docs/Tutorials/创建四类模块教程.md` 和 `开发计划.md` 同步启动数据流与示例。
3. 最终复核发现 `onWindowHandleReady()` 仍无条件发送 `showWindow`。现已把控制帧
   收口到“用户/布局要求显示且 UI ready”的统一分支；明确隐藏时不补显示，之后
   用户勾选仍会补发。
4. 已按 `git ls-files "*.md"` 核查全部 9 个受版本控制 Markdown，并完成下表记录。

### 命令与结果

Debug x64 使用以下串行命令构建成功：

```powershell
$env:TEMP='D:\QFrameworkv2\.tmp\msbuild'
$env:TMP='D:\QFrameworkv2\.tmp\msbuild'
& 'D:\Program Files (x86)\Microsoft Visual Studio\2017\Community\MSBuild\15.0\Bin\MSBuild.exe' `
  'D:\QFrameworkv2\QFramework.sln' /t:QFrameworkTests /m:1 `
  /p:Configuration=Debug /p:Platform=x64 `
  /p:QtInstallDir='D:\Qt\5.15.2\msvc2019_64' `
  /p:TrackFileAccess=false /v:minimal
```

晚到窗口修正并补充真实 `ProcessUiExample` 子进程用例后的 10 项直接测试结果
`12 passed, 0 failed`（含 init/cleanup，12893 ms）。新增用例先在窗口未 ready 时
加载显示布局，再加载明确隐藏布局并异步重启，直接检查两轮原生窗口可见性以及
之后菜单补显示；没有增加生产测试钩子。最终完整 Debug 测试结果
`48 passed, 0 failed, 0 skipped`（24768 ms）。

Release x64 使用同一命令构建整个 `QFramework.sln`，去掉 `/t:QFrameworkTests`
并把配置改为 `/p:Configuration=Release`，结果成功；最终完整测试结果
`48 passed, 0 failed, 0 skipped`（20904 ms）。Debug/Release 测试均显式使用
`QT_QPA_PLATFORM=windows`。构建只有既有
`windeployqt: VCINSTALLDIR is not set` 非致命警告。第一次收口前的 Release 测试
出现过一次 `QWindowsPipeWriter: asynchronous write failed（管道已结束）`，该项
测试及全套仍通过；重新构建后的最终完整运行没有再出现该诊断。

源码审计命令覆盖 `QThread::terminate`、`terminate()`、`processEvents`、`wait()`、
`Qt::BlockingQueuedConnection`、`std::thread/std::mutex`、两个废弃条件变量和
`stopAcknowledged`。结果确认消息线程无 terminate、无 BlockingQueuedConnection、
无嵌套 processEvents、无新增标准库线程原语；`ProcessRuntime::MessageQueue` 和
Logger/MessageBus 保留的条件变量均有实际等待者。Logger Worker 的既有
`terminate()` 兜底仍存在，按第 24.3 节分类记录，不误报为全仓删除。

最终 `git diff --check` 退出码 0；输出仅包含 Git 对现有 LF 工作区文件未来可能
转换为 CRLF 的提示，没有空白错误。最终 `git status --short` 仍保留用户原有
`.gitignore` 修改和未跟踪 `修改建议.txt`，未回滚、覆盖或纳入本轮自动清理。

### 最终 Markdown 审计

| 文档 | 结果 |
| --- | --- |
| `修改计划.md` | 已同步第 16～20 批实际实现、测试、代码漂移和剩余人工门槛。 |
| `修改进度.md` | 已同步实际文件、命令、结果、诊断和本审计表。 |
| `开发计划.md` | 已同步 onStart 契约、异步重启/停止、Dock 意图和最终自动化结果。 |
| `Docs/Explanation/架构说明.md` | 已同步启动数据流、线程归属、状态机、quarantine、统计和 Dock pending。 |
| `Docs/Guides/操作与调试指南.md` | 已同步菜单、标签布局、异步重启和卡死诊断。 |
| `Docs/Reference/模块开发接口参考.md` | 已同步 onStart、publish、协作停止、线程与隔离契约。 |
| `Docs/Reference/框架配置文件参考.md` | 已同步独立 deadline、总退出预算和 RestartWindow `>=` 边界。 |
| `Docs/Tutorials/创建四类模块教程.md` | 已同步 onStart 期间发布示例及失败撤销说明。 |
| `Docs/文档索引.md` | 已核查，链接和文档描述仍准确，无需修改。 |

### 桌面验收排查与剩余门槛

1. 使用 `computer-use` 尝试枚举窗口，插件连续返回
   `EnumWindows failed: 系统找不到指定的路径`；其技能文档要求的
   `sky.documentation()` 也与当前插件导出不一致。
2. 通过可见 `Start-Process` 启动 Release 应用后，QFrameworkApp、ProcessUi 和
   ProcessNonUi 均运行且 `Responding=True`，但 PowerShell 的
   `MainWindowHandle` 与 User32 `EnumWindows` 都没有任何顶层 HWND。
3. 清理测试实例后显式设置 `QT_QPA_PLATFORM=windows` 再启动，结果仍相同；第二个
   测试实例及子进程也已清理。
4. 完成性审计后的第三轮再次在 active console Session 1 可见启动 Release：主进程
   PID 2620、ProcessUi PID 9636、ProcessNonUi PID 14916 均 `Responding=True`，但
   `MainWindowHandle=0`；`computer-use` 仍缺少技能要求的 `sky.documentation()`，
   `list_windows()` 仍报 `EnumWindows 0x80070003`，独立 User32 枚举返回
   `WindowCount=0`。三个 PID 的可执行文件路径核对为工作区 Release 目录后均已
   清理，无残留测试实例。

因此仍需在真正可交互的 Windows 桌面人工验证：菜单勾选/取消、标签切换、布局
保存/加载、ProcessUi 显示意图为真时晚到补显示、明确隐藏时不补显示，以及重启
期间拖动主窗口和打开菜单仍可响应。当前记录没有把 windows 平台自动测试或无
HWND 的进程存活当成人工通过；硬件相关门槛本次范围内也未执行。

## 2026-08-05：ProcessUi 嵌入尺寸与重启容器加固

状态：代码、真实子进程直接测试和直接相关文档已完成；本轮完整 Debug/Release、
最终差异审查和用户桌面复验仍在进行，以下不提前写成全部通过。

### 复现证据与代码漂移

用户在真实桌面确认原菜单、标签、布局和 GUI 响应均可操作后，提供四张截图：
ProcessUi 内容右侧/底部露出宿主背景；连续缩放后子窗口偶发失效，重新加载会出现
重复横条并把框架拉长。核对发现 `applyRequestedDockVisibility()` 只在显示时调用
一次 `showWindow(width, height)`，Dock 的后续 `resizeEvent` 没有父子尺寸同步；
`clearEmbeddedWindow()` 又会在 Restarting 状态同步删除外部窗口容器。

### 实际修改

1. `Source/QFramework/Ui/ProcessWindowHost.h/.cpp`：新增 50 ms 单次 `QTimer`、
   `clientSizeChanged(QSize)`、稳定 size hint、`QSizePolicy::Ignored`、零容器最小尺寸；
   旧容器改为隐藏、移出布局后 `deleteLater()`。
2. `Source/QFramework/Ui/MainWindow.h/.cpp`：连接宿主尺寸信号，仅在显示意图、ready、
   `Running` 和 Dock 可见同时成立时转发；隐藏和重启状态不发送尺寸帧。
3. `Source/QFramework/Process/ProcessSupervisor.h/.cpp`、`ProcessRuntime.cpp`：新增
   `resizeWindow` 帧；`showWindow` 与 `QWidget::resize()` 解耦，保留旧带尺寸重载作为
   先 show、再 resize 的兼容入口。
4. `Tests/QFrameworkTests/tst_Baseline.cpp`：真实 HWND 覆盖首次尺寸、连续缩放、
   重启恢复、隐藏 resize 不补显示和主窗口最小尺寸边界。

### 当前命令与结果

- Debug x64 `QFrameworkTests` 目标构建成功；第一次 MOC 构建发现并修正一个
  `private slots` 区段边界错误，重跑后成功。只有既有
  `windeployqt: VCINSTALLDIR is not set` 非致命警告。
- `processIpcAndSupervision processUiLateWindowHonorsLayoutVisibility`：
  `4 passed, 0 failed`（含 init/cleanup，14584 ms）。
- 加入停止闸门、控制帧失败、统计、restart 边界和 Socket 线程归属后的 7 项选择：
  `9 passed, 0 failed`（含 init/cleanup，14212 ms）。测试显式使用
  `QT_QPA_PLATFORM=windows`，没有把 offscreen 当成人工通过。

### 当前文档审计

- 已同步：`修改计划.md`、`修改进度.md`、`Docs/Explanation/架构说明.md`、
  `Docs/Guides/操作与调试指南.md`。
- 已核查，当前行为不改变公开模块 SDK 或 INI：`Docs/Reference/模块开发接口参考.md`、
  `Docs/Reference/框架配置文件参考.md`、`Docs/Tutorials/创建四类模块教程.md`；无需修改。
- 待完整 Debug/Release 成功后再更新：`开发计划.md` 的最终自动化结果。
- `Docs/文档索引.md` 链接和描述仍准确，无需修改。

## 2026-08-05：ProcessNonUi 超长消息撑大 UI 的复现与修复

状态：根因已由用户稳定复现、日志和失败测试共同确认；两个 UI 示例、直接回归以及
完整 Debug/Release 已完成，最终差异审查和真实桌面复验仍未提前标记为全部通过。

### 复现证据

- 用户路径：InProcessUi 与 ProcessUi 左右停靠，点击模块管理中的 ProcessNonUi 重启；
  主窗口变长且无法恢复。
- `QFramework_2026-08-05_000.log` 在 16:27:17、16:27:29 记录 ProcessNonUi
  启动/重启，未记录 ProcessUi 重启。
- 旧实现新增回归失败：InProcessUi `minimumSizeHint().width()` 为 `12466`，
  ProcessUi 原生客户区为 `12482x320`，宿主要求为 `500x320`。

### 实际修改、命令与结果

- 修改 `Modules/InProcessUiExample/InProcessUiExample.cpp`、
  `Modules/ProcessUiExample/ProcessUiExample.cpp`：完整消息仍解码、计数和传输，
  QLabel 只显示有界预览，并隔离最小尺寸约束。
- 修改 `Tests/QFrameworkTests/tst_Baseline.cpp`：新增超长日志最小尺寸断言和
  ProcessNonUi 手动重启期间 ProcessUi HWND/尺寸断言。
- 失败基线命令：`QFrameworkTests.exe pluginLoaderAndModuleIntegration
  processIpcAndSupervision`，结果 `2 passed, 2 failed`，失败值见上。
- 修复后同一 Debug 命令，结果 `4 passed, 0 failed, 0 skipped, 0 blacklisted`，
  约 8 秒。
- 修复后完整 Debug `QFrameworkTests.exe`：`48 passed, 0 failed, 0 skipped, 0 blacklisted`，
  约 28 秒；完整 Release：同样 `48 passed, 0 failed, 0 skipped, 0 blacklisted`，
  约 24 秒。Release 全解决方案重新构建成功，只有既有 `VCINSTALLDIR` 非致命警告。

剩余门槛：`git diff --check`/最终状态审查，以及真实桌面上的左右停靠、ProcessNonUi
重启、连续缩放和两个布局文件复验。
