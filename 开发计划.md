# QFramework 开发计划

## 文档状态

- 状态：待实施。
- 本文件是架构与实施计划，不是源代码、构建、运行或 GUI 验收已经完成的证据。
- 新会话开始时必须先执行“阶段 A：只读基线审计”，再开始任何实现。
- 如旧文档、旧脚本或旧记录与本文件冲突，以本文件和用户最新指令为准。

## 1. 目标与边界

从零构建一个仅支持 Windows 10/11 的 Qt 模块框架，名称为 `QFramework`。

固定技术条件：

- Qt 5.15.2；
- C++17；
- Visual Studio 2017 与 Visual Studio 2026；
- `v141` 工具集；
- Qt 的 `msvc2019` / `msvc2019_64` 构建包；
- Protobuf 3.21.12；
- Debug 使用 `/MDd`，仅保证在装有 Visual Studio 调试环境的开发机运行；
- Release 使用 `/MD`，作为可复制的便携式发布目录；
- 不制作安装程序。

框架包含四类模块：

| 模块类型 | 文件形式 | 运行位置 | 是否有主 Dock |
| --- | --- | --- | --- |
| `InProcessUi` | DLL | 主进程 | 是 |
| `InProcessNonUi` | DLL | 主进程 | 否 |
| `ProcessUi` | EXE | 独立子进程 | 是，使用 HWND 嵌入 |
| `ProcessNonUi` | EXE | 独立子进程 | 否 |

主程序只负责框架启动停止、主窗口、菜单、Dock、配置、消息总线和模块管理；禁止把业务逻辑写入主程序。

## 2. 总体架构

```text
QFrameworkApp.exe
├─ 配置、日志、布局、全局 QSS
├─ 中央 MessageBus
├─ QPluginLoader
│  ├─ InProcessUi DLL
│  └─ InProcessNonUi DLL
└─ QProcess
   ├─ ProcessUi EXE
   └─ ProcessNonUi EXE

子进程 <-> QLocalSocket / QSharedMemory <-> 主进程中央 MessageBus
```

所有模块间消息均经主进程中央 `MessageBus` 路由。模块之间不建立直接 Socket、共享内存或 RPC 连接。`QLocalSocket` 和 `QSharedMemory` 只是框架内部的传输实现，模块 API 只接触主题和 `QByteArray`。

核心工程依赖：

```text
QFrameworkProtocols.dll
          ↑
QFramework.dll
          ↑
QFrameworkApp.exe、主进程 DLL 模块、子进程 EXE 模块
```

`QFrameworkSdk` 只保存公开头文件和必要宏定义，不单独生成 DLL 或 LIB。

## 3. 目录和输出约定

```text
QFramework\
├─ QFramework.sln
├─ Source\
│  ├─ QFramework\
│  │  ├─ Core\
│  │  ├─ Config\
│  │  ├─ Logging\
│  │  ├─ Messaging\
│  │  ├─ Plugin\
│  │  ├─ Process\
│  │  └─ Ui\
│  ├─ QFrameworkApp\
│  ├─ QFrameworkSdk\
│  └─ QFrameworkProtocols\
│     ├─ Protos\
│     └─ Generated\
├─ Modules\
│  └─ <模块名>\
├─ config\
│  └─ QFramework.ini
├─ ThirdParty\
│  └─ Protobuf\3.21.12\
├─ Build\
│  ├─ Intermediate\
│  └─ Lib\v141\<架构>\<配置>\
├─ Bin\v141\<架构>\<配置>\
├─ Tests\
├─ Tools\
└─ Docs\
```

规则：

- 所有项目加入唯一的 `QFramework.sln`；
- 仅维护 `.sln` 和 `.vcxproj`，使用 Qt Visual Studio Tools，不维护 qmake 或 CMake；
- 不使用仓库自定义 VS 模块模板；模块通过 VS 的“添加新项目”创建；
- 模块源码必须放在 `Modules\<模块名>\`，内部的 Qt 默认模板结构、中间目录由模块开发者自行安排；
- 框架项目中间文件放在 `Build\Intermediate`；模块自己的中间文件可自行设置；
- 最终 DLL、EXE、PDB、Qt 运行库、Protobuf 运行库放在对应的 `Bin` 目录；
- 模块运行文件建议统一放在 `Bin\v141\<架构>\<配置>\Plugins\<模块ID>\`；
- `QFrameworkApp` 构建后调用匹配 Qt 版本的 `windeployqt`；
- 配置、布局和其他运行文件由开发者手工复制到运行目录，不实现自动复制。

## 4. 模块 SDK 与生命周期

提供 UI 和非 UI 两个基类。UI 基类继承 `QWidget`，非 UI 基类继承 `QObject`。两者共享相同的生命周期和消息 API。

建议公开接口：

```cpp
virtual QStringList publishedTopics() const;
virtual QStringList subscribedTopics() const;

virtual bool onStart();
virtual void onStop();

virtual void onMessage(const QString& topic,
                       const QString& senderModuleId,
                       const QByteArray& data);

bool publish(const QString& topic, const QByteArray& data);

void logDebug(const QString& text);
void logInfo(const QString& text);
void logWarning(const QString& text);
void logError(const QString& text);
```

接口规则：

- `publishedTopics()` 与 `subscribedTopics()` 默认返回空列表；
- 模块启动前注册主题；首版不支持运行时订阅或取消订阅；
- `onStart()` 默认返回 `true`；返回 `false` 时该模块失败、记录错误并弹窗，但其他模块继续；
- `onStop()` 默认空实现；
- `onMessage()` 默认空实现；
- 主进程模块由框架持有；UI 模块的最终 QWidget 父子链由框架拥有的 `QDockWidget` 管理；
- 主进程 UI 与非 UI 模块的生命周期回调在主进程 Qt 主线程执行；
- 每个模块有独立消息队列和消息线程，`onMessage()` 在该模块消息线程执行；
- UI 模块不得在 `onMessage()` 直接操作界面。应发射信号，再以显式 `Qt::QueuedConnection` 调用 GUI 线程槽函数；
- 模块可以从自己的任意线程调用线程安全的 `publish()`；
- 模块停止前后调用 `publish()` 均返回 `false` 并记录警告；
- 不核对模块 API 版本；模块和框架的二进制兼容性由开发者自行保证；
- 不支持主进程 DLL 热卸载或热重载。更新 DLL 后必须重启主程序；运行中关闭 UI Dock 仅隐藏模块；
- C++ 异常应在框架回调边界记录并隔离；严重原生内存错误可能导致主进程退出，此限制已经接受。

子进程的 `main.cpp` 保持最小：创建 `QApplication` 或 `QCoreApplication`、创建模块实例，并调用框架提供的子进程运行入口。

## 5. 主题与 Protobuf

主题使用字符串宏，例如：

```cpp
#define message_modules_a "message_modules_a"
```

规则：

- 可以有多个主题头文件，也可以有多个 `.proto` 文件；
- 一个主题通常只对应一种消息结构，但框架不校验映射；
- 发布方手动 `SerializeToArray()` 或 `SerializeAsString()`，接收方手动解析；
- 框架只传递主题、发送者模块 ID 和 Protobuf 序列化字节；
- 多个发布者和多个订阅者均允许；
- 模块可收到自己发布的消息，并通过发送者模块 ID 进行业务排除；
- 同一发送者的同一主题保持顺序；不同发送者之间不保证全局顺序；
- A 在 B 尚未订阅或尚未加载时发布消息，B 不会补收该消息；
- 未在 `publishedTopics()` 中声明的主题调用 `publish()` 时，框架拒绝发布、返回 `false` 并记录警告。

Protobuf 固定使用 3.21.12：

- `.proto` 放在 `Source\QFrameworkProtocols\Protos\`；
- `protoc.exe` 生成文件到 `Source\QFrameworkProtocols\Generated\`；
- `.pb.cc` 只在 `QFrameworkProtocols.dll` 中编译一次；
- 模块只包含具体 `.pb.h`，并链接 `QFrameworkProtocols.lib`；
- `ThirdParty\Protobuf\3.21.12\` 保存头文件、x86/x64 库、`protoc.exe` 和发布需要的 `libprotobuf.dll`；
- 运行包同时携带对应架构的 `libprotobuf.dll`；
- `ThirdParty` 同时保存 v141 x86/x64 VC++ Redistributable 安装包。

## 6. 消息总线与队列

消息总线只提供异步发布/订阅，不提供同步 RPC。

内部设计：

- 主进程中有一个中央路由器；
- 每个已启动模块有独立输入队列与消息线程；
- 小消息使用 `QLocalSocket`；
- 超过 `SharedMemoryThresholdBytes` 的消息使用 `QSharedMemory` 传输；
- 模块始终接收完整 `QByteArray`，共享内存创建、读取、释放和异常清理由框架处理；
- 子进程连接由每次启动生成的随机令牌验证，令牌不写入 INI 和日志；
- 子进程不能仅凭模块 ID 冒充已配置模块。

队列策略：

- 每个订阅者独立限流；一个订阅者队列满不影响其他订阅者；
- 默认可靠主题队列满时不阻塞，发布返回 `false`；
- 流式大数据主题队列满时丢弃旧数据，保留最新数据；
- 单条消息有可配置最大大小，禁止真正无上限；
- `QFramework.ini` 提供全局默认队列容量、大小限制、共享内存阈值和默认策略；
- 只有少量特殊主题在 `[Topic.<主题>]` 中覆盖全局值；
- 启动、停止、未注册主题、大小超限、可靠队列满等失败必须以 `bool` 返回并记录日志；
- 单条 Protobuf 解析失败、流式丢帧和普通队列满只记录日志与计数，不反复弹窗；
- 程序退出或模块停止时先停止接受新消息，在 `ShutdownDrainTimeoutMs` 内尽量排空已有消息；超时后丢弃剩余消息并记录数量；
- 子进程自动或手动重启前，清空其未处理消息，不做持久化补发。

## 7. 模块加载与子进程监督

模块加载由 `config\QFramework.ini` 控制，严格按 `[Modules]` 从上到下顺序加载。

`[Module.<模块ID>]` 至少包含：

- `Enabled=true/false`；
- `Type=InProcessUi|InProcessNonUi|ProcessUi|ProcessNonUi`；
- `DisplayName`；
- 模块文件路径。

`Enabled=false` 时框架不加载 DLL、不启动 EXE，也不参与自动重启。

主进程 DLL 使用 `QPluginLoader` 和 `Q_PLUGIN_METADATA`。子进程 EXE 只能由框架使用 `QProcess` 启动，不支持作为普通独立程序运行。

对子进程实现：

- 注册握手超时 `RegistrationTimeoutMs`；
- 心跳间隔与心跳超时；
- 正常停止超时后强制结束；
- 异常退出、注册超时和心跳超时均按故障处理；
- 自动重启延迟；
- 在 `RestartWindowMs` 内最多自动重启 `MaxRestartCount` 次；
- 超限后停止自动重启、记录错误、弹窗提示；
- 用户可以对故障状态和正常运行状态的子进程执行手动重启；
- 手动重启清空自动重启次数；
- 子进程 UI 重启期间保留原 Dock 位置并显示占位内容。

Debug 子进程支持每模块配置：

- `WaitForDebugger=true`；
- `DebuggerWaitTimeoutMs`。

启用后，子进程在执行 `onStart()` 前等待开发者在 Visual Studio 附加进程。超时仍未附加时继续启动并记录警告。Release 忽略此调试等待配置。

同一程序目录只允许运行一个 `QFrameworkApp.exe`。重复启动时提示“程序已在运行”并退出；不同程序目录允许独立运行。

## 8. UI、Dock、布局与对话框

主窗口首次启动保持空白。UI 模块可以已经加载和启动，但其 Dock 默认隐藏。

Dock 规则：

- 用户第一次显示某个 UI 模块时，默认停靠左侧；
- 多个模块默认可以叠放为标签页；
- 拖动过程中允许临时浮动预览；
- 松开鼠标后必须重新停靠，不能长期浮动；
- 关闭 Dock 只隐藏模块，不停止模块；
- 主进程 UI 模块的 Dock 由框架拥有；
- 子进程 UI 使用 Windows HWND 嵌入其框架 Dock；子进程自行负责其内部 QWidget 父子关系；
- UI 模块允许创建模态和非模态业务对话框；
- 子进程模态对话框只阻塞自身子进程，不阻塞主窗口和其他模块；
- 非 UI 模块需要提示用户时，通过消息通知合适的 UI 模块显示对话框。

布局文件扩展名为 `.qflayout`：

- 支持启动加载、运行中加载、保存和另存为；
- 保存 Dock 位置、标签页、尺寸、显示/隐藏状态和主窗口几何信息；
- 只有用户点击保存时才写文件；退出时不自动保存；
- 运行中加载其他布局后，“保存当前布局”覆盖当前活动布局文件；
- 程序绝不修改 `QFramework.ini`；
- 缺失模块跳过并继续恢复；
- 默认布局缺失或损坏时，主窗口保持空白并继续启动；
- 建议使用 JSON 保存布局元数据和 Base64 编码的 `QMainWindow::saveState()` 数据。

## 9. QSS

首版只支持一份全局外部 QSS：

- 程序启动时只加载 INI 指定的 QSS；
- 运行中可选择任意绝对或相对路径的外部 `.qss`；
- 支持重新加载当前 QSS；
- 运行中选择的 QSS 仅保存在内存，不写回 INI，不影响下次启动；
- 主进程 UI 与子进程 UI 使用同一全局样式；
- 样式切换时通过框架通知子进程 UI；
- QSS 加载失败时保留当前样式、记录日志并弹窗；
- 不提供框架界面内的 QSS 编辑和保存功能；
- 布局与 QSS 完全独立。

## 10. 配置与日志

固定运行配置：

```text
config\QFramework.ini
```

支持的 INI 分组：

```ini
[Modules]
[Module.<ModuleId>]
[MessageBus]
[Topic.<TopicName>]
[Process]
[Layout]
[Style]
[Logging]
```

要求：

- INI 中每个变量必须写中文注释，说明用途、单位、默认值和适用范围；
- 所有 QFramework.ini 相对路径统一相对该 INI 所在的 `config` 目录解析；
- 布局和 QSS 可使用任意绝对路径；
- 主程序不修改 INI；修改 INI 后必须重启；
- 主进程 DLL 的业务配置相对主程序 EXE 目录；
- 子进程 EXE 的业务配置相对自身 EXE 目录；
- 子进程工作目录设置为自身 EXE 目录。

日志要求：

- 所有日志集中写入 `Logs`；
- 提供自动注入模块 ID 的 `logDebug/logInfo/logWarning/logError`；
- 使用全局 Qt 日志处理器捕获 `qDebug/qWarning/qCritical`；无法确定来源时模块 ID 写为 `Unknown`；
- 日志包含毫秒时间、级别、模块 ID、线程 ID 和文本；
- 日志按日期和文件大小滚动，例如 `Rolling=DateAndSize`；
- 旧日志不自动删除；
- 框架状态性故障（模块启动失败、子进程重启超限、布局/QSS 无法加载等）记录日志并弹窗；
- 高频业务故障仅记录日志和计数。

## 11. Visual Studio 配置与调试

所有 `.vcxproj` 内联保存工具集、Qt、Protobuf、运行库、宏、输出和链接配置；不创建仓库自定义共享 `.props` 文件。Qt Visual Studio Tools 自己生成的 Qt 属性和目标文件允许保留。

每个模块项目需要的文档化配置包括：

1. 在解决方案中添加 Qt 默认 DLL 或 EXE 项目；
2. 选择 Qt 5.15.2 对应架构版本；
3. 设置 `v141`、C++17、`/MDd` 或 `/MD`；
4. 添加 `QFrameworkSdk`、Protobuf 和生成协议头文件的包含目录；
5. 添加 `QFramework.lib`、`QFrameworkProtocols.lib`、Protobuf 库；
6. 配置模块输出目录和 PDB 输出目录；
7. UI DLL 添加 `Q_PLUGIN_METADATA` 并继承 UI 基类；
8. 非 UI DLL 继承非 UI 基类；
9. 子进程 EXE 使用框架提供的最小运行入口；
10. 在 QFramework.ini 注册模块。

调试结论：

- 主进程 DLL 被 `QPluginLoader` 加载后，Visual Studio 中该 DLL 的断点可以命中；
- 子进程 EXE 通过 `WaitForDebugger` 停在 `onStart()` 前，开发者可在 VS 使用“附加到进程”后命中初始化断点；
- Debug 运行目录必须保留所有 PDB；
- VS2026 编辑 `v141` 项目是可行的，但必须安装 v141 工具集；
- Qt 包名为 `msvc2019` 不等于自动使用 v142/v143，必须实际验证 VS2017 + v141 与该 Qt 包组合。

## 12. 示例、测试和文档

提供四个最小示例模块：

1. 主进程 UI DLL；
2. 主进程非 UI DLL；
3. 子进程 UI EXE；
4. 子进程非 UI EXE。

示例必须演示主题注册、Protobuf 手动序列化、消息发布/接收、日志、Dock、信号槽切回 GUI 线程、模态/非模态对话框、子进程重启、子进程调试和布局/QSS 操作。

建立一个 Qt Test 自动化测试项目，至少覆盖：

- INI 解析、相对路径和 INI 不被修改；
- 模块加载顺序与 `Enabled=false`；
- 主题注册、未声明主题拒绝、自身消息、同发送者顺序；
- 队列容量、可靠/流式策略、大小上限和停止排空；
- `QLocalSocket` 与 `QSharedMemory` 消息路径；
- 子进程令牌、注册超时、心跳、自动重启和手动重启；
- 布局保存/加载/缺失模块；
- QSS 切换、重新加载和失败恢复。

文档要求：

- 全部面向中文读者；代码标识符使用英文；
- 为“新建四类模块后的全部配置”编写图文教程；
- 所有截图使用 VS2026；每一张截图必须有中文标注；
- VS2017 仍需进行构建和调试验收；
- 文档分为教程、操作指南、API/INI 参考和架构说明四部分。

最终验收矩阵：

| Visual Studio | 架构 | 配置 |
| --- | --- | --- |
| VS2017、VS2026 | Win32、x64 | Debug、Release |

以上共八种组合均需完成构建和人工 GUI 运行验证。性能测试记录吞吐量、延迟、消息大小、订阅者数量和丢帧数，但首版不设置硬性性能阈值。

## 13. 分阶段实施

### 阶段 A：只读基线审计

1. 检查当前目录、Git 状态和已有文件；
2. 检查 VS2017、VS2026、v141、Windows SDK、Qt 5.15.2、Protobuf 3.21.12 是否可用；
3. 记录 Qt 的 `msvc2019` / `msvc2019_64` 路径、`protoc.exe` 路径和 `windeployqt` 路径；
4. 不在该阶段声称任何项目已构建或运行；
5. 发现计划与真实环境冲突时，先报告证据并请求用户决定。

通过条件：输出清晰的环境与工作区基线，不修改无关文件。

### 阶段 B：解决方案与基础工程

1. 创建 `QFramework.sln`；
2. 创建 `QFrameworkProtocols`、`QFramework`、`QFrameworkApp`、Qt Test 项目；
3. 建立目录、输出目录、导出宏和基础资源；
4. 设置 v141、C++17、Qt、Protobuf、Debug/Release 运行库；
5. 验证空工程可构建。

### 阶段 C：SDK、协议、配置和日志

1. 实现 UI/非 UI 基类和公共接口；
2. 实现多 `.proto` 生成及 `QFrameworkProtocols.dll`；
3. 实现主题头文件与手动 Protobuf 使用示例；
4. 实现只读 INI、路径解析和完整注释示例；
5. 实现异步日志、Qt 全局日志捕获和滚动文件。

### 阶段 D：主进程模块和进程内消息总线

1. 实现 `QPluginLoader`、DLL 元数据、加载顺序和 `Enabled`；
2. 实现生命周期、错误隔离和模块状态；
3. 实现中央路由器、模块队列、消息线程和主题校验；
4. 实现大小限制、队列策略、顺序和停止排空；
5. 用主进程 UI/非 UI 示例完成第一轮集成测试。

### 阶段 E：子进程 IPC 与监督

1. 实现最小子进程运行入口；
2. 实现 `QProcess`、随机令牌注册、`QLocalSocket` 协议；
3. 实现大消息 `QSharedMemory` 路径和清理机制；
4. 实现注册超时、心跳、停止、自动重启和手动重启；
5. 实现等待调试器功能；
6. 用子进程 UI/非 UI 示例完成 IPC 集成测试。

### 阶段 F：UI、布局和样式

1. 实现主窗口、左侧模块管理入口和状态显示；
2. 实现主进程 UI Dock、子进程 HWND 嵌入和重启占位；
3. 实现不可长期浮动的拖拽停靠约束；
4. 实现 `.qflayout` 保存、加载、另存为和异常恢复；
5. 实现全局 QSS 加载、切换、重新加载和子进程通知。

### 阶段 G：示例、文档和验收

1. 完成四个示例模块；
2. 完成 Qt Test 自动化测试；
3. 编写 VS2026 标注截图教程和 VS2017 差异说明；
4. 在八种 VS/架构/配置组合中构建；
5. 手工验证四类模块、断点、Dock、布局、QSS、异常重启和便携式运行目录；
6. 只根据新的构建、测试和 GUI 证据声明完成。

## 14. 新会话执行原则

新会话的开发代理应当：

1. 先完整阅读本文件；
2. 先做阶段 A 的只读审计，不能把此计划当作已实现；
3. 使用 Qt 风格实现，尽量少用 lambda；
4. 只在必要范围修改文件，保留用户已有改动；
5. 每个阶段完成后执行最小相关构建或测试，并报告真实结果；
6. 不得因旧计划、旧日志或旧记忆而宣称构建、测试、GUI 或部署已完成；
7. 遇到真实环境与计划冲突时，给出证据、影响和最小可行调整方案，再请求用户决定；
8. 全程使用简体中文说明，代码标识符保持英文。
